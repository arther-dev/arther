/**
 * Stub (Phase 2 G4.4). One renderer, three outputs: editor preview, portal
 * SSR, and PDF via @media print (ADR-008; architecture §4). The 20 block
 * types and the TipTap JSON tree (ADR-013) land here with migration 0005.
 */
export type BlockRendererPlaceholder = never;
