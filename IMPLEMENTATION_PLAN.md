# Arther — Implementation Plan

**Date:** 11 June 2026 · **Status:** Active · **Owner:** repo root

This is the execution roadmap for building Arther v1 from the completed specification suite. It synthesizes — and links to, rather than restates — the PRD, the 18 feature specs, the architecture/ADR set, the data model and migrations, the four phase task docs, and the design handoff.

---

## 0. How to use this document

- **This doc** answers: what gets built, in what order, what "done" means per phase, which decisions are pinned, and what is still open.
- **The phase task docs** ([Phase 1](Development/Architecture/arther-phase1-tasks.md) · [Phase 2](Development/Architecture/arther-phase2-tasks.md) · [Phase 3](Development/Architecture/arther-phase3-tasks.md) · [Phase 4](Development/Architecture/arther-phase4-tasks.md)) remain the **canonical task lists** (42 epics, ~213 tasks, with acceptance criteria). This doc does not duplicate them.
- **Feature behavior** lives in [`Features/Spec Docs/`](Features/Spec%20Docs/). **Visual/interaction spec** lives in [`Development/Handoff/`](Development/Handoff/). **Schema** lives in [`supabase/migrations/`](supabase/migrations/) (canonical) with the documented reference copy in [`Development/Architecture/migrations/`](Development/Architecture/migrations/).
- If this doc and a source doc disagree, the source doc wins — fix this doc.
- Every working session appends one line to [§10 Session log](#10-session-log).
- **Before picking a task, check open PRs and `claude/*` branches** — not just this log. The session log reflects only **merged** work, so an open PR is in-flight, *not* done. Full routine in [CLAUDE.md → Autonomous task routine](CLAUDE.md).

## 1. Source-of-truth index

| Doc family | Path | Role |
|---|---|---|
| Product overview | [`Features/PRD/arther-product-overview.md`](Features/PRD/arther-product-overview.md) | Why Arther exists; positioning |
| PRD | [`Features/PRD/arther-prd.md`](Features/PRD/arther-prd.md) | v1 scope; **§8 invariants**; **§9 build order**; deferred list |
| Feature specs (18) | [`Features/Spec Docs/`](Features/Spec%20Docs/) | Authoritative feature behavior |
| Architecture | [`Development/Architecture/arther-architecture.md`](Development/Architecture/arther-architecture.md) | Topology, module map, data flows, cost posture, **§15 revisit triggers** |
| ADRs 001–014 | [`Development/Architecture/arther-adrs.md`](Development/Architecture/arther-adrs.md) | Locked stack decisions + rationale |
| Data model | [`Development/Architecture/arther-data-model.md`](Development/Architecture/arther-data-model.md) | ~60 entities, conventions (RLS, attribution, immutability) |
| Migrations (15) | [`supabase/migrations/`](supabase/migrations/) (canonical) ⇄ [`Development/Architecture/migrations/`](Development/Architecture/migrations/) (reference) | Phase-ordered executable schema (0012 field-value RPC · 0013 releases/overrides · 0014 membership governance · 0015 import commit) |
| Architecture audit | [`Development/Architecture/arther-architecture-audit.md`](Development/Architecture/arther-architecture-audit.md) | 20 findings, all resolved 2026-06-09 |
| Phase tasks (4) | `Development/Architecture/arther-phase{1..4}-tasks.md` | Canonical epics/tasks/acceptance/milestones |
| Design handoff | [`Development/Handoff/`](Development/Handoff/) | DS foundations, app shell, 12 surfaces, a11y wiring |
| Design audits | [`Design/`](Design/) | DS/a11y/QA audits (all remediated as of 2026-06-09) |
| Launch gate | [`Development/Architecture/vibecode-best-practices.md`](Development/Architecture/vibecode-best-practices.md) | Pre-launch checklist (Phase 4 H.5) |

## 2. Locked stack decisions

Per the [ADR decision summary](Development/Architecture/arther-adrs.md#decision-summary): TypeScript everywhere (001) · Next.js App Router for app + portal (002) · modular monolith, two front doors, one monorepo (003) · Postgres as the single datastore (004) · Supabase for DB/Auth/Storage/RLS (005) · Trigger.dev for durable jobs (006) · Claude called directly via one `ai-gateway` call site, no provider abstraction (007) · Playwright/Chrome PDF off the shared renderer (008) · Vercel Domains API for custom domains (009) · `canDo` + RLS defence in depth (010) · Resend email (011) · Zod as the one schema source (012) · TipTap/ProseMirror with atom inline spec tokens (013) · Upstash Redis for rate limits and ephemeral caches (014).

**Build-time pins the ADRs don't cover** (recorded here; revisit triggers noted):

| Pin | Value | Rationale / revisit if |
|---|---|---|
| Node / pnpm | 22 (`.nvmrc`) / pnpm 10 (`packageManager`) | Active LTS; revisit at Node 24 LTS |
| TypeScript | 5.x, `strict: true`, no cross-package path aliases | Workspace deps keep Turborepo graphs honest |
| Next.js / React | latest stable major, pinned in lockfile at scaffold time | Recorded in root `package.json`; revisit per major |
| Tailwind | **v4** (CSS-first `@theme` over v3 `theme.extend`) | Native CSS-variable theming matches the two-tier token system; [Handoff 01 §10](Development/Handoff/01-foundations-design-system.md) intent preserved (one var source) |
| Unit tests | Vitest | Single runner across packages |
| DB tests | `postgres` (postgres.js) against dockerized Postgres 17 | Same probes run in CI service container |
| Lint | ESLint 9 flat config from `packages/config` | `eslint-config-next` layered in apps |
| Monorepo runner | Turborepo 2.x | Per ADR-003 |
| E2E tests | Playwright `~1.56` (`tests/e2e`, chromium) against production builds of both apps | Same engine as the ADR-008 PDF pipeline; every shipped surface gets at least a render/interaction smoke. Pinned to the sandbox's preinstalled chromium-1194 (`PLAYWRIGHT_BROWSERS_PATH`); CI installs its own browsers — bump freely once dev environments can download them |

## 3. Repo layout convention

**Application code lives at the repo root, beside the doc folders. `Design/`, `Development/`, `Features/` stay exactly where they are** — the doc suite is densely cross-linked by relative path; moving it breaks links for zero benefit.

```
apps/app          authenticated workspace (Next.js)        apps/portal      public portal (Next.js)
packages/types    branded IDs, enums, Zod schemas          packages/db      data-access layer (user vs service client)
packages/authz    canDo() + role/seat model                packages/ui      design tokens + DS components
packages/block-renderer  one renderer: editor/portal/PDF   packages/jobs    Trigger.dev tasks (Phase 2+)
packages/ai-gateway  the single Claude call site (ADR-007) packages/spec-import  SpecReconciler + import pipeline
packages/config   env loader, eslint preset                packages/rate-limit  Upstash limits + in-memory fallback (ADR-014)
scripts/          local DB harness + checks                supabase/migrations  canonical executable SQL
tests/db          smoke + RLS probes
```

**Migrations canonicality rule:** [`supabase/migrations/`](supabase/migrations/) (timestamp-prefixed, per the [migrations README](Development/Architecture/migrations/README.md)) is what gets applied — locally, in CI, and via `supabase db push` once projects exist. [`Development/Architecture/migrations/`](Development/Architecture/migrations/) is the frozen, documented reference the phase docs link to. `scripts/check-migration-drift.sh` fails CI if they diverge. A genuine schema fix updates **both** and says so in the commit.

## 4. Execution roadmap

Sixteen milestones across four phases. Critical path for v1:

```
F0→F1→F2→F3→F4→F5  →  G0→G1→G2→G3  →  C0→C1→C4→C5→C6  →  H
   (F6, F7 ∥ after F5)   (G4, G6 ∥ after G3)  (C2→C3 ∥, early; C7→C8 after C6)   (R ∥ V; then A; then K)
   F8 continuous gate     G8 continuous gate    C9 continuous gate                  H continuous gate
```

| Milestone | Phase | Epics | Outcome | Exit criteria | Cloud deps activated |
|---|---|---|---|---|---|
| M1 Tenancy spine | 1 | F0–F4 (+F8.1 early) | Sign up, workspace, invite | Second-user RLS probe green | Supabase, Vercel, Sentry |
| M2 Spec graph | 1 | F5, F6.1–6.3 | Hand-edit specs with history | Shared-component history; override semantics | — |
| M3 Import | 1 | F7 | Real spreadsheets in | Real `.xlsx` → correct field graph → release | Anthropic (import), Trigger.dev |
| M4 Harden & dogfood | 1 | F6, F8 | Spec DB dogfooded | [Phase 1 DoD](Development/Architecture/arther-phase1-tasks.md) | Upstash (F8.2) |
| M5 Generate | 2 | G0–G3 | Spec + type → Draft block tree | Zero-hallucination validation rejects unresolvable tokens | Anthropic + Trigger.dev (full) |
| M6 Edit | 2 | G4–G5 | Three-panel editor, auto-save, offline queue | 20 block types, one renderer | — |
| M7 Keep in sync | 2 | G6–G7 | Staleness routed to domain owners | Field change → correct dashboard item, published untouched | — |
| M8 Harden | 2 | G8 | Dogfood generation | [Phase 2 DoD](Development/Architecture/arther-phase2-tasks.md); 100+-block volume test | — |
| M9 Sign-off | 3 | C0–C3 | Review/approve/notify in-app | AND-logic approvals; unified notifications only | Resend |
| M10 Publish | 3 | C4–C6 | Frozen snapshot → PDF → SSR portal | Snapshot immutability probe; PDF ready-gate | Portal deploy (Vercel) |
| M11 Gate & brand | 3 | C7–C8 | Access tiers, magic links, custom domains | Allowlist + revocation behaviors | Vercel Domains API |
| M12 Launch-ready lifecycle | 3 | C9 | Dogfood-published | [Phase 3 DoD](Development/Architecture/arther-phase3-tasks.md); portal-isolation probe | — |
| M13 Reuse & variants | 4 | R ∥ V | Snippets propagate; variant families publish | Live transclusion; delta resolution + merge | — |
| M14 Measure | 4 | A | Analytics surfaces | Events → per-doc panel + admin dashboards | — |
| M15 Guide | 4 | K | Ask Arther + onboarding | Write actions gated by confirmation + `canDo` | — |
| M16 v1 launch-ready | 4 | H | v1 | Full RLS probe; [launch-readiness audit](Development/Architecture/vibecode-best-practices.md) passes | — |

**Sequencing notes** (from PRD §9/§13 and the phase docs): C3 notifications are built early in Phase 3 because Phase 2 staleness alerts and review requests deliver through them (invariant 8). Document Types and Brand Profiles (G0) sit at the Phase 1/2 seam — schedule after the Spec Database ships so it can be dogfooded first. G6.9 and the deferred FKs (H.1) keep variants additive.

## 5. Definition of done per phase

| Phase | DoD (per phase doc) | Proven by |
|---|---|---|
| 1 | Sign up → workspace → invite → import a real Excel sheet into a version-controlled product/component graph → edit with history → named release | RLS probe (F8.1) green in CI; real spreadsheet import; immutability triggers verified |
| 2 | Select product + type + brand → AI generates a grounded Draft (atomic, zero-hallucination) → edit in block editor → spec change flags the right owner in minutes | Generation rejects unresolvable tokens; staleness routes via domain-ownership fallback; published content untouched |
| 3 | Submit → AND-logic approvals → publish → frozen versioned snapshot on a branded SSR portal with PDF; gated docs need a magic link | Snapshot-immutability probe; portal-isolation probe; PDF ready-gate; access logging |
| 4 | Snippets propagate live; variant-aware generate/merge/publish with portal picker; analytics live; assistant answers and acts with confirmation | Full RLS probe across all tables; launch-readiness audit YES |

## 6. Cloud dependency activation schedule

No cloud credentials exist yet. Everything below the "needed at" line is buildable and verifiable locally first — dockerized Postgres 17 + the local auth shim (`scripts/sql/0000_local_auth_shim.sql`), env-gated client factories that throw typed "not provisioned" errors, and mocked gateway interfaces.

| Provider | First needed at | Locally buildable before credentials |
|---|---|---|
| Supabase | F0.2 (M1) | Full schema + RLS + RPCs against docker Postgres via the auth shim; data-access layer; probes |
| Vercel | F0.3 (M1) | Both Next apps build/run locally |
| Sentry | F0.4 (M1) | Config stub, DSN-gated |
| Anthropic | F7.2 (M3), full at G1 (M5) | `ai-gateway` interface + Zod tool-use schemas; mocked in tests |
| Trigger.dev | F7 (M3), full at G1 (M5) | `packages/jobs` task definitions; invoked directly in tests |
| Resend | F4.3 / C3 (M9) | Notification model + dispatch interface; email templates |
| Upstash | F8.2 (M4), G8.5, C9.4 | Rate-limit wrapper interface; in-memory fallback in dev |

## 7. Open build-time questions & recommended resolutions

| # | Question | Source | Recommendation | Decide by |
|---|---|---|---|---|
| 1 | Reviews queue: do Approved/Published docs stay listed? | Handoff 03 §E | Stay listed behind a default `status=needs_action` filter (cheap; preserves audit visibility) | Phase 3 C2 |
| 2 | Settings schema-editor depth (Document Type editor) | Handoff 00/04 | v1 = bounded structural editing (reorder sections, map categories, toggle `brief_required`, rename); built-ins fork-not-edit (matches G0.1/G0.2) | Phase 2 G0 |
| 3 | New Document live-preview fidelity | Handoff 00/03 §C | Reuse `block-renderer` read-only, append per section; fallback = status-stream rows; never pixel-true brand preview at generation time | Phase 2 G2.8 |
| 4 | Panel-toggle keybindings `⌥⌘\` / `⌥⌘⇧\` *(tbc)* | Handoff 02 §6 | Adopt as written (`⌘\` = Focus is settled); revisit on usability evidence | Phase 2 shell |
| 5 | Admin override for unresolved domain reviews | Architecture §15 | v1: none beyond owner approval-override; revisit on observed deadlock | Phase 3 |
| 6 | Tailwind v4 vs v3 | Handoff 01 §10 | **v4** (see §2) — deviation from the doc's literal `theme.extend` wording, intent preserved | ✅ Decided (this plan) |
| 7 | Trigger.dev task location (docs silent) | Architecture §8 | `packages/jobs` (own `trigger.config.ts` in Phase 2; apps import task types only) | ✅ Decided (this plan) |
| 8 | `canDo` module location | Phase 1 F3.1 | `packages/authz` — importable by apps *and* jobs so the single-call-site rule holds | ✅ Decided (this plan) |
| 9 | Local DB strategy | Migrations README | Plain `postgres:17` + minimal auth shim (deterministic, CI-identical); adopt `supabase start` when F0.2 provisions projects | ✅ Decided (this plan) |
| 10 | Light-mode structural token values (not in repo) | Handoff 01 §2 | Ship dark theme; `[data-theme="light"]` stubbed pending Figma Dev Mode variables export | Before Phase 2 editor paper island |
| 11 | uuid v7 PKs | `0001_conventions.sql` | Keep `gen_random_uuid()` v4 (no native v7 in PG17); revisit at PG18 | Deferred |
| 12 | Portal CSP vs. static caching | F8.3 | App + portal ship a nonce-based `strict-dynamic` CSP (middleware), which forces dynamic rendering. Fine for the app (personalised) and the portal *stub*; published-doc pages must be CDN-cacheable (C6), so switch the **portal** to a build-time hash-based (or static-nonce) CSP when cached published docs land | Phase 3 C6 |

## 8. Standing verification gates (every PR)

1. `pnpm turbo lint typecheck test build` across all packages and both apps.
2. Migration apply against a `postgres:17` service container (`ON_ERROR_STOP`), auth shim first.
3. DB smoke probes: helpers exist, `create_workspace()` seeds defaults, immutability triggers raise, slug immutable, archive guards block.
4. **Second-user RLS probe** (F8.1, pulled forward from the Phase 1 gate): user B in workspace W2 can neither read nor mutate W1 rows. Extended at G8.3 (Phase 2 tables), C9.1/C9.2 (snapshots/portal), H.2 (all tables).
5. Migration drift check: doc copy ≡ canonical copy.
6. **Playwright E2E suite** (`tests/e2e`) against production builds of `apps/app` and `apps/portal`. Standing rule from M1 on: every feature PR that adds or changes a user-facing surface adds/updates an E2E spec for it (auth flows at F2, spec database UI at F6, import at F7, editor at G4, portal + no-JS readability at C6, …).

## 9. Risk register

| Risk | Mitigation / fallback | Trigger to act |
|---|---|---|
| Chrome-in-Playwright PDF flakiness (architecture §15: "flakiest dependency") | Managed Browserless | Sustained PDF job failures in C5 |
| RLS policy cost on graph/staleness queries | Indexed denormalised `workspace_id`; measured in G8.4 | Volume test misses latency targets |
| Realtime fan-out limits for generation progress | Poll `generation_run_sections` fallback | Realtime instability at M5 |
| Figma ↔ code token drift | Tokens live in one `tokens.css`; re-export from Dev Mode on DS republish | Any DS republish |
| Doc-copy ↔ canonical migration drift | CI drift check (§8.5) | Any red drift check |
| TipTap ↔ React major compatibility | Pin TipTap at G4.3; smoke editor build on framework upgrades | Next/React major bumps |
| Missing light-mode token values | §7.10 — export before Phase 2 editor | Editor paper island work starts |
| Cloud provisioning gap — **infra gate closed 2026-06-11** (F0.2–F0.4 ran for real; F0.4 acceptance source-mapped, ARTHER-APP-3) | Residual: Supabase auth-config confirmation (dashboard) + PITR plan decision; M1 proper still gates on F0–F4 + second-user RLS probe | Real prod data (PITR), F2 auth verification |

## 10. Session log

| Date | Session | Shipped | Deviations |
|---|---|---|---|
| 2026-06-11 | Phase 1 kick-off | This plan; monorepo scaffold (apps/app, apps/portal, 7 packages); canonical migrations + docker Postgres harness + auth shim; smoke + RLS probes; `canDo` + seat model; dark design tokens + starter atoms; Zod env loader; CI | `packages/authz`/`packages/jobs` added beyond F0.1's named list (§7.7–7.8); F1.6 lint rule deferred to follow-up (runtime guard + tests in place); F0.2–F0.4 deferred pending credentials |
| 2026-06-11 | Playwright E2E | `tests/e2e` Playwright suite (chromium, prod builds of both apps via webServer) + CI `e2e` job; standing gate §8.6 — every user-facing surface ships with an E2E smoke | — |
| 2026-06-11 | Cloud wiring (code side) | Sentry SDK in `apps/app` (DSN-gated, PII scrubbed, source-map upload token-gated); `supabase/config.toml` for CLI link/push; per-app `vercel.json` (turbo-ignore); `PROVISIONING.md` runbook for the account-side F0.2–F0.4 | Account-side provisioning pending — Supabase/Vercel/Sentry MCPs not visible in this session (connectors attach at session start); M1 infra gate closes after the runbook is executed |
| 2026-06-11 | Provisioning (PROVISIONING.md executed) | Supabase: `arther-prod` created; all 11 migrations applied to dev + prod, verified (60 tables, RLS 60/60, seeds, RPCs). Sentry: `arther/arther-app` project + DSN. Collected values + status recorded in PROVISIONING.md | Prod region us-west-1 (connector doesn't offer dev's us-west-2). Manual remainder per PROVISIONING.md status: Vercel repo import ×2 + env vars (connector has no create/env tools), Supabase auth config + service-role keys, Sentry auth token; PITR needs paid plan. M1 stays open until F0 acceptance on a real URL |
| 2026-06-11 | F5.4 groundwork + app shell | FieldValue union for all 8 spec field types in `@arther/types` (Zod, ADR-012: one source for editors/AI contracts/import) + §3.5 override rules; app-shell skeleton per Handoff 02 — TopBar/LocalRail/AppShell in `@arther/ui`, `/dashboard` + `/specs` routes with the region matrix, home → `/dashboard`, token sheet → `/design-tokens`; 6 new E2E shell specs | Shell is the static frame: tab system, ⌘K palette, navigator/inspector behavior land with F6/G4; placeholder glyphs pending the DS icon export |
| 2026-06-11 | F2 auth (code side) | Auth surfaces per the auth IA — `/login` `/signup`(+`/verify`) `/forgot` `/reset/{token}` `/welcome` `/invite/{token}` as a branded card outside the shell; `@supabase/ssr` middleware (session refresh + routing) and server actions (logIn/signUp/logOut/reset/createWorkspace via the 0003 RPC/Google OAuth), Zod-validated, all env-gated with typed not-provisioned degradation; `TextField` DS atom with the label/aria-describedby contract; `slugifyWorkspaceName` + live portal-slug preview; 7 E2E auth specs | Full auth flows verifiable end-to-end once the manual provisioning items land (Supabase auth config + env vars); invite acceptance renders the honest dead-end until F4.3; Profile/account-menu surfaces follow with Settings (F4.5) |
| 2026-06-11 | F5/F6 first slice | Migration **0012** `update_spec_field_value()` (atomic version append + pointer move, invoker-rights RLS; both copies + README) with 5 DB probes; `@arther/db` spec repository (workspace/products/fields/units/versions, Zod-gated writes, `membershipLookupFor` canDo wiring); `formatFieldValue` + `wouldCreateReferenceCycle` (F5.9) in `@arther/types`; `/specs` UI — product navigator + creation, categorized field grid, add-field (all 8 types), inline scalar editor; canDo-gated server actions | Data-bearing UI E2E needs provisioned env (unprovisioned baseline preserved); remaining F6 editors (range/toleranced/enum/multi_enum/table/reference), components/graph UI, releases, overrides, version feed = next slices |
| 2026-06-11 | F5/F6 second slice | Per-type inline editors for the full scalar family (range/toleranced/boolean/enum/multi_enum; enum options defined at field creation per §4.6); **Component Library** at `/specs/library` (F5.2) with create + per-component field grids + "used in N products" badges; product↔component **graph edges** (F5.3) — attach-with-quantity, component sections on the product view with shared-component affordances (F6.4 start); rail views are real links now | Table editor (mini-spreadsheet, F6.3 L) and reference picker still pending; releases (F5.7), overrides (F5.6), version/comment feed (F6.5) next; component nesting (parent_component_id) deferred with tree rendering |
| 2026-06-11 | M1 infra verification + turbo env fix | Verified manual provisioning: Vercel `arther-app`+`arther-portal` live with env vars (F0.3 done), `SENTRY_AUTH_TOKEN`/`SUPABASE_SERVICE_ROLE_KEY` set. F0.4 acceptance run 1 reached Sentry (ARTHER-APP-1/2) with **unmapped** frames; root-caused from Vercel build logs (turbo strict env stripped undeclared `SENTRY_*`/`SUPABASE_*` from `app#build`; pnpm blocked `@sentry/cli` postinstall) and fixed: `turbo.json` build `env`+`passThroughEnv`, `@sentry/cli` in `onlyBuiltDependencies` (PR #10). Run 2 on the PR preview: build logged `Successfully uploaded source maps` ×3, probe → **ARTHER-APP-3 source-mapped** (`page.tsx:16 SentryCheck`) — **F0.2–F0.4 infra gate closed**; HTTPS app URL verified (auth-gated). CI green ×4 on PR | M1 proper still gates on F0–F4 + second-user RLS probe (needs Supabase auth config — dashboard-only, unverifiable via connector). PITR still blocked on free plan |
| 2026-06-11 | F5/F6 third slice — releases + overrides | Migration **0013** (both copies): `create_product_release()` RPC — atomic snapshot pinning the current version of every valued product + attached-component field (invoker rights); release **delete** policy + guard (0003 shipped no delete path; spec §3.8 wins — blocked only while `block_spec_references.release_id` lineage exists); type-change guard while overrides exist (§3.5); override integrity guard (scalar family only, field must belong to the edge's component). Repo: release list/create/delete, override list/set/clear (Zod-gated). UI: Releases section on the product page (create + confirm-delete), `/specs/releases` rail view wired, **Edit (global) vs Override (this product)** affordances on shared-component rows with override chip + global value (F6.4). 14 DB probes, 3 new E2E specs (22/22 green; shell spec updated — Releases rail item is a link now) | "Which products hold overrides" surface arrives with the type-change UI (none exists yet; the 0013 guard holds the invariant meanwhile); release detail (pinned-value list) deferred; table/reference editors remain the next F6.3 slice |
| 2026-06-12 | F6.3 complete — table + reference editors | **Table mini-spreadsheet** (spec §5.5): column mapping (name/role/unit per column, schema roles enforced client-side then by Zod), numeric rows, **Excel/CSV paste** via pure `parseTablePaste` in `@arther/types` (TSV/CSV, header detection, NBSP normalization; 5 unit tests), interpolation select, **live chart preview** through new `SpecChart` in `@arther/ui` (pure SVG, structural props — the Phase 2 Chart block renders through the same primitive). **Reference picker**: select over the Component Library (self excluded), value renders as a navigable `→ name` link; **F5.9 cycle check at save** — `listReferenceEdges` (component-owned reference fields) + `wouldCreateReferenceCycle` in the action, own edge excluded on re-point; added the missing reference-graph unit tests (5). `SpecFieldRow` now carries `component_id`/`product_id` | All 8 field editors now live. Data-bearing editor E2E still pending the provisioned-E2E environment (unit + DB-probe coverage meanwhile); searchable combobox + side-panel reference preview and drag-to-reorder are F6.1-polish items; series promotion UX (§5.5 #2) approximated by per-column role selects |
| 2026-06-12 | F5/F6 closed — comments, feed, detail panel, archive, nesting | **F5.8 field comments**: composer + repo (`addFieldComment` captures `field_version_id` + `value_snapshot` server-side at insert — "at this comment" can never drift); commenting routed through canDo **`comment.write`** (viewer right; `authorize()` parameterized). **F6.5 unified feed**: value changes + comments interleaved chronologically with author attribution (`listUsersByIds`) and per-comment value context. **F6.1 detail panel**: `?field=` server-rendered `FieldDetail` on `/specs` and `/specs/library` — field names link in; header shows type/category/archived + current value. **F5.10 archive UI**: `setArchived` for products/components/fields + restore disclosures (archived products on the product page, archived components + per-component archived fields in the library); hard delete stays DB-guarded, no UI. **F6.2 nesting**: attach gains "Nest under" (edge-under-edge `parent_component_id`), components render as a recursive tree. 5 new DB probes (viewer-can-comment RLS, version-context persistence, threading, stranger isolation, archive editor-gating) | Phase 1 F5/F6 epics now closed except: drag-to-reorder (display_order edit UI), comment threading UI (schema + probe ready; composer is flat), searchable reference combobox + side-panel preview — all F6-polish, parked for the dogfood pass (M4). Provisioned-data editor E2E still pending the env |
| 2026-06-12 | F2 closure + F4 workspace admin | **F2.3 PKCE callback** `/auth/callback` (code exchange → `next`; middleware allows `/auth`; signup/recovery/OAuth all route through it — links dead-ended before); `APP_URL` in env schema + Vercel (owner set A+B+C: email-confirm on, Google OAuth, site URL — PROVISIONING updated). Migration **0015→0014** (both copies): owner-rules trigger (**exactly one owner**; closes the 0002 gap where admins could mint `role='owner'`), atomic `transfer_workspace_ownership()` (definer, GUC-scoped trigger bypass), `get/accept_workspace_invitation()` definer RPCs (invitee is RLS-blind pre-membership). **F4.2/F4.4/F4.5**: `/settings` — rename, immutable-slug display, members table (role select / remove / transfer with confirm), role-aware rendering; account menu (Settings + Log out) behind the TopBar avatar. **F4.3**: invitations live end-to-end — create (+copyable 7-day link), revoke, `/invite/[token]` accept page; Resend delivery is one env-gated fetch that activates with `RESEND_API_KEY`. 8 DB probes; settings/account-menu E2E; shell spec: Account control is a labeled `<summary>` now | M1's remaining proof is human: signup → email confirm → `/welcome` → workspace on the real URL (owner test). Logo upload (Storage) + Danger Zone surface → F8.7; login `next`-return after invite → polish. 0014 to apply to cloud dev+prod post-merge |
| 2026-06-12 | Fix: auth redirects derive from request host | Prod Google sign-in bounced to /login: GoTrue logs showed `/callback`→`user_signedup`/`login` (200/302) but the app never saw `/auth/callback` — the browser landed on `/` then `/login`, i.e. GoTrue **rejected `redirect_to` and fell back to the Site URL**. Root cause: redirects built from a single static `APP_URL` (wrong/absent on prod, and structurally wrong for per-deploy preview hosts). Fix: `appOrigin()` reads `x-forwarded-host`/`-proto` so OAuth `redirectTo` + email-confirm/recovery `emailRedirectTo` + invite links target the exact host (APP_URL kept only as non-request fallback); new-but-workspaceless users (first Google sign-in) now redirect `/dashboard`→`/welcome`. Email "not received" was a red herring — `user_repeated_signup`: the Google sign-up had already created callumkelpin@gmail.com, so the email signup sent nothing (anti-enumeration). Gates 29/29, E2E 25/25 | Belt-and-suspenders still owner-side: confirm prod redirect allowlist carries the `https://arther-app.vercel.app/**` wildcard. Built-in Supabase SMTP is rate-limited — custom SMTP (Resend) is the production path (rides the same key as F4.3) |
| 2026-06-12 | M1 sign-in spine verified LIVE | Google OAuth end-to-end on production after the redirect fix + the `https://arther-app.vercel.app/**` allowlist entry (owner added). Evidence: Vercel runtime logs show `GET /auth/callback 307` (was `GET /`→`/login` pre-fix); prod DB confirms the full chain — first sign-in → `/welcome` → `create_workspace` (0003 RPC) produced workspace **SPAN** with the user as **owner** (atomic membership + seeded defaults). Pre-fix orphaned auth record cleared. Sign-up → workspace = **M1 core acceptance, proven on real infra** | Invite-by-**email** loop is the last M1 "invite" gate — needs Resend (the link flow works today; Supabase built-in SMTP is unreliable). Then F7 import (Anthropic + Trigger.dev). F8.1 RLS probe has been green in CI throughout |
| 2026-06-15 | F8.3 — security headers (M4 hardening gate) | Shared, framework-agnostic `@arther/config/security`: `STATIC_SECURITY_HEADERS` (HSTS w/ preload+includeSubDomains, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy` deny-by-default, COOP, DNS-prefetch off) + `buildContentSecurityPolicy(nonce, {app,isDev})` — nonce + `strict-dynamic` strict profile, dangerous directives locked (`default-src/object-src/frame-ancestors/base-uri`), app gets Supabase+Sentry `connect-src`/Google `form-action`, portal stays tight, dev relaxes script-src for HMR, `upgrade-insecure-requests` in prod. Both apps' middleware mint a per-request nonce, inject it into request headers (Next stamps its scripts) and set CSP + baseline on every response (app: woven into the existing Supabase session flow incl. redirects; portal: new middleware). Both root layouts → `force-dynamic` so the per-request nonce reaches static pages. 12 config unit tests + 6 E2E header assertions across both prod builds (fresh-nonce-per-response, no `unsafe-eval`). Gates 35/35 + E2E 35/35 (slug-preview hydration proves client JS runs under the strict CSP) | Nonce CSP forces dynamic rendering — recorded as §7 Q12: the **portal** must move to a hash-based/static-nonce CSP at C6 so published docs stay CDN-cacheable (no-JS readability already holds — SSR content). F8.2 rate-limiting / F8.7 Danger-Zone surface / F8.4 PITR remain the open F8 items |
| 2026-06-15 | F8.2 follow-up — throttle invite-accept | Wired the existing `authThrottle()` (shared `auth` IP budget, `@arther/rate-limit`) into `acceptInviteAction` — the one auth surface PR #20's F8.2 left unthrottled, so invitation-token guessing now degrades like every other unauthenticated auth action. Closed the duplicate F8.2 PR #21 (a parallel limiter in `@arther/config`, superseded by the merged `@arther/rate-limit`); carried forward only its invite-accept coverage. Placed after the not-provisioned check, before the Supabase/RPC call (pattern-consistent). Gates `pnpm turbo lint typecheck test build` green | No new bucket/surface (reuses the `auth` budget by IP) so no type/API change and no E2E; covered by the package's existing unit tests. Remaining F8: F8.5 input-validation sweep, F8.6 single-handler audit, F8.7 Danger Zone; F8.4 PITR blocked on a paid plan |
| 2026-06-12 | F7 — AI-powered import (M3, code-complete) | **`@arther/spec-import`** — the SpecReconciler shared service (§6.4): xlsx/csv parse (exceljs + own RFC-4180 CSV), zod/v4 interpretation contract (raw units as written; tables as source ranges — numbers come from the file, never the model), unit normalisation w/ conservative alias fold (mV≠MV), additive-by-default reconcile (✓/~/+/− plan, stable keys), decisions layer (skip/rename/re-unit/re-categorise), F7.5 warnings (unrecognised unit → null value + reading preserved in conditions, duplicates disambiguated, value-not-in-source grounding check); 21 unit tests. **`@arther/ai-gateway`** — the one Claude call site (ADR-007/invariant 9): structured outputs via `zodOutputFormat`, adaptive thinking, streaming, token logging, env-gated typed errors; 5 tests. Migration **0015** `commit_import_session()` (atomic plan apply via 0012/0013 + auto-release "Imported from {file}"; both copies + README rows for 0013–0015); 6 DB probes (atomicity, nesting, rollback-on-failure, double-commit guard, viewer/stranger isolation). App: `/specs/import` full-canvas stepper (upload → structural review → field review → validation → diff-first commit), re-import entry on the product page + empty-state link, recent-sessions list, honest not-provisioned/failed states w/ retry-from-storage; 4 E2E specs (29/29 green). Cloud: 0015 applied to dev+prod; `spec-imports` Storage bucket + workspace-scoped policies (editor upload/member read) on dev+prod via MCP | F7 acceptance ("real spreadsheet → correct field graph") needs **ANTHROPIC_API_KEY** in Vercel (owner step, PROVISIONING.md) — flow degrades honestly until then. Durable-job wrapper (ADR-006) deferred to G1: interpretation runs in the server action (`maxDuration` 300). zod **v4** confined to the AI-contract schemas (SDK helper requirement); repo stays on classic v3. Type corrections in review deferred (type conflicts surface read-only; edit in Specs post-import); templates (§6.1/6.3) are not in the F7 epic. New packages beyond §3's original list recorded in §3 |
| 2026-06-14 | F8.2 — rate limiting (M4 harden) | New **`@arther/rate-limit`** package: Upstash sliding windows (`@upstash/ratelimit`, ADR-014) behind one `rateLimit(name, id)` API, with a per-instance in-memory sliding-log fallback when Upstash is unprovisioned or briefly unreachable — correct with Redis down (degrades, never fails open or locks users out). Three named budgets wired to the abuse-prone surfaces (F8.2 scope): **auth** 10/60s by client IP (login/signup/reset/OAuth), **invitation** 20/60s by inviter, **import** 5/60s by member (the paid AI call — upload + retry). Auth throttle sits *after* the not-provisioned check so unprovisioned/E2E envs never consume budget; new `clientIp()` helper reads `x-forwarded-for`. `UPSTASH_*` added to turbo `passThroughEnv`; transpilePackages + app workspace dep wired. 5 unit tests (deterministic clock); gates 38/38, E2E 29/29, drift in sync | No browser E2E for the limiter — it's a behavioral guard (not a new surface), and a shared-suite trip on per-instance in-memory state would be flaky; unit-tested instead. Upstash creds are the owner step (PROVISIONING.md, plan §6) — runs on the in-memory fallback until set. Remaining Phase-1 exit gate: F8.3 (security headers), F8.5 (input-validation sweep), F8.6 (single-handler audit), F8.7 (workspace deletion); F8.4 (PITR) still blocked on a paid plan |
| 2026-06-15 | F8.5 — input-validation sweep (M4 harden) | Swept every external boundary in `apps/app`: all 4 server-action files (~20 actions across specs/import/settings/auth) already Zod-`safeParse` their `FormData` at the boundary, and `/auth/callback` Zod-guards `next` against open redirect — input-validation half confirmed closed by prior incremental work. Least-privilege responses: zero `.select('*')` in the codebase (every query names columns); closed the one real leak — `createReleaseAction` + `commitImportAction` returned raw `e.message`, which `@arther/db` prefixes with the internal call-site name and could carry raw Postgres text (constraint/column names). New `@arther/db` `DbRuleError` + `rpcError(context, error)`: app-raised rules (SQLSTATE **P0001**, the author-written `raise exception` messages) surface verbatim and safe; everything else stays a prefixed `Error` the action folds into a generic message. Wired through `createRelease`/`deleteRelease`/`commitImportSession`; the two actions now surface only `DbRuleError`. Durable guard: root ESLint `no-restricted-syntax` bans `.select('*')` (DOM-safe, runs under `turbo lint`) so least-privilege selects stay enforced. 3 `errors.test.ts` cases (P0001 verbatim / 23505 internal / unknown-SQLSTATE internal). Gates `pnpm turbo lint typecheck test build` 38/38 green | `deleteReleaseAction` already had a generic fallback (no change needed). No new DB probe — the change is JS error-typing, not SQL behavior (the dockerized probes already cover the RPCs' `raise exception` paths). **Remaining Phase-1 exit gate: F8.6 (single-handler audit); F8.4 (PITR) still blocked on a paid plan** |
| 2026-06-15 | F8.5 + F8.6 — input-validation sweep + single-handler audit (last Phase-1 exit gates) | **F8.5:** audited all 4 server-action files (28 actions) + the lone route handler + both middlewares — every boundary already `safeParse`s with Zod and returns only `*FormState` (no raw rows; least-privilege holds). Closed the real gaps the sweep surfaced: (1) the import **review actions** (`saveStructural/FieldDecisionsAction`) built `decisions` from user-controlled form keys and persisted it with Zod running only on *read* — now re-validated through `importDecisionsSchema` at the **write** boundary (rejects before `import_sessions.decisions` is touched); (2) every free-text input was unbounded (`text` columns, `.min(1)` with no ceiling) — new shared `@arther/types` `text` module (`TEXT_LIMITS` + `requiredText`/`optionalText`/`emailField`, ADR-012 one-source) caps names/categories/notes/tag/comment/email/password across the auth, settings, and specs schemas + the spec-import decision schema; (3) the PKCE `/auth/callback` now validates `code`/`next` via Zod (the open-redirect guard re-expressed as `nextSchema.catch('/dashboard')`). **F8.6:** traced every lifecycle trigger (signup email+OAuth, invite create/accept, password reset, ownership transfer, workspace create/delete) across actions, RPCs, and `pg_trigger` — the "one handler per trigger" invariant **holds** (one `on_auth_user_created`→`handle_new_user`; one owner-rules guard; membership inserted only by `create_workspace`/`accept_workspace_invitation`; no duplicate triggers). New `tests/db/single-handler.test.ts` (4 probes) locks the DB half in CI so a future duplicate trigger fails the gate. New `text` unit tests + `importDecisionsSchema` bound tests. Gates `pnpm turbo lint typecheck test build` 38/38, E2E 35/35, drift in sync | DB probe authored against the migration set but **not run locally** (no docker in this session — it runs in CI's Postgres service container, like the other `tests/db` probes). Decision-schema length caps are mirrored as local constants in `@arther/spec-import` (kept dependency-light) rather than importing `@arther/types`. **Phase-1 F8 epic now closed except F8.4 (PITR) — still blocked on a paid Supabase plan.** With F8.5/F8.6 done, the only remaining Phase-1 item is the human M1 invite-by-email loop (needs Resend) per the prior log entries |
| 2026-06-15 | G0.6 — Product Briefs (Phase 2 kick-off, no migration) | First Phase-2 task taken on an axis no open PR touches: **#28/#29/#30 all re-implement G0.1 Document Types** (colliding on migration 0017) and **#31 does G0.4 Brand Profiles** — G0.6 Product Briefs is independently buildable on `main` and, like #31, ships as a **pure management layer over the 0004 tables (no new migration → no 0017 collision)**. The Brief is the generator's narrative layer (spec §3.2/§5.7), mirroring the spec graph — fragments live on a **product OR a component**, so a shared component's narrative reaches every product that references it. **`@arther/types` `brief.ts`** (ADR-012, one source): `BRIEF_FRAGMENTS` catalogue — the 5 standard keys (spec §3.2) + every key the 0004 built-in Document Types reference — each with Arther-defined **guidance** (spec §5.7: guidance is baked-in, not user-configurable); `briefFragmentKeySchema` (lowercase-slug shape, not an allow-list, so custom types stay free) + bounded plain-text body (F8.5, `TEXT_LIMITS.briefFragment`); `orderBriefKeys`/`briefKeyLabel`/`briefGuidance` helpers. **`@arther/db` `briefs.ts`**: `getEntityBrief`, `upsertBriefFragment` (lazy brief-row create on first write, race-safe re-read; PostgREST upsert on `(brief_id,key)` — empty body clears via `deleteBriefFragment`), and `listBriefKeyUsage` (which Document Type sections reference each key — the "referenced by" panel + the expected-key list, reading built-in + workspace types). **App**: a `[Spec Fields] / [Product Brief]` tab on the product page (`?tab=brief`) and a full-canvas component brief on the library (`?component=&tab=brief`, "shared brief" banner) — fragment list with completeness (✓/○) + per-key editor (textarea), guidance, referenced-by, last-edited attribution; brief writes are editor-gated (`spec.write`, matching 0004 RLS); `?fragment=`/`?component=` validated at the boundary (F8.5, degrades not 500s). 10 brief unit tests; 6 DB probes (`tests/db/briefs.test.ts` — editor write / viewer read-only / one-brief-per-entity + one-fragment-per-key uniqueness / **component brief visible to every member** = the G0 acceptance / stranger isolation); 3 E2E. Gates `pnpm turbo lint typecheck test build` 38/38, E2E 38/38, drift in sync (16 files, **no migration**) | "Needed by N **documents**" placeholder counts (spec §5.7) need the block model — deferred to G3; surfaced "referenced by N **sections**" (computable now) meanwhile. Within a product's brief tab, component briefs are reached via the library link-through rather than inlined (avoids N brief fetches; the generator assembles graph briefs at G2). Logo/rich-text intentionally absent — briefs are plain text by spec. **Open G0 after this: G0.1 (pick one of #28/#29/#30, close the rest), G0.2 sections, G0.3 approval-role assignments, G0.5 Quality Standards.** |
| 2026-06-15 | G0.4 — Brand Profiles (Phase 2 G0, independent axis) | First **Phase 2** slice on an axis clear of the contested G0.1 pileup (PRs #28/#29/#30 all re-implement Document Types; none merged). Brand Profiles stand alone — `document_types.default_brand_profile_id` points *into* them — so they're buildable on `main` today. **No migration:** the 0004 `brand_profiles` table + RLS (member read / owner-admin write) + `brand_profiles_one_default_idx` partial unique index already shipped; this is the management layer. **`@arther/types`** `brand-profile.ts` (ADR-012 one source): `BrandProfileId` brand, `brandProfileFormSchema` (every free-text field bounded per F8.5; hex-colour + URL guards), and pure parsers `parseStringList`/`parsePreferredTerms`/`formatPreferredTerms` for the free-text→JSONB shapes (11 unit tests). **`@arther/db`** `brand-profiles.ts`: list (default-first, with per-profile "N document types reference this" counts), create (**first profile auto-becomes the workspace default**, spec §7.3), update, **set-default** (clears the existing default *before* setting the new one so the partial unique index never sees two), **archive** (refuses the last live profile; promotes a sibling to default first so the workspace is never default-less), restore — all single-table over the user-JWT client (no RPC; reused `canDo('workspace.manage')`, no authz change). **App** `/settings/brand-profiles` (list + create + make-default + archive + archived-restore disclosure) and `[id]` editor (logo URL, palette, heading/body fonts, voice descriptors, tone notes, preferred/prohibited glossary, unit preference; id validated at the boundary per F8.5) + a "Branding" link from `/settings`. 6 DB probes (owner/admin write + member/viewer read-only, single-default index incl. archived-doesn't-count, clear-then-set, cross-tenant isolation) + 2 E2E (unprovisioned baseline + malformed-id-never-500). Gates `pnpm turbo lint typecheck test build` 38/38, drift in sync (16 files) | Chose an independent G0 task over a 4th duplicate G0.1 — **owner must pick one of #28/#29/#30 to merge and close the others** (all collide on migration 0017 + `/settings/document-types` + a `doctype.manage`/`workspace.manage` authz choice). Logo is a URL field for now — Storage upload deferred with the workspace-logo item (F4.5). Glossary `preferred_terms` edited as `term => preferred` lines. Default-toggle + archive-promote are app-side JS (not atomic over PostgREST, acceptable for an admin settings action; the partial-unique-index invariant is the DB-enforced guarantee and is probe-covered). DB probes authored against the migration set, run in CI's Postgres service container (no docker this session). G0.3 approval-role *assignments*, G0.5 Quality Standards, G0.6 Product Briefs remain the next G0 slices |
| 2026-06-15 | G0.1/G0.2 — Document Types admin (Phase 2 kick-off, M5 prerequisite) | First Phase-2 slice: the generation schema the AI generator fills (generator spec §3.4). **`@arther/types`** `document-types`: `BLOCK_TYPES` (the 20-type set, kept in lockstep with the 0005 `blocks.type` CHECK — a drift test guards it), `STANDARD_BRIEF_FRAGMENT_KEYS`, `parseTokenList`, and `documentTypeSectionInputSchema` (the section data contract — categories→section map, brief keys, brief-required, default block types), validated at the write boundary (ADR-012, one source). Migration **0017** (both copies + README) `fork_document_type()` — atomically clones a Document Type (row + sections + approval roles) into a workspace as an editable copy (built-in → fork, or duplicate); invoker rights, 0004 admin-write RLS governs. **`@arther/db`** repository: list (built-ins + workspace), get-with-sections, create-from-scratch, fork, rename, archive/restore, and section create/update/delete/reorder. App `/settings/document-types` (admin-only, rail-less Settings mode): built-ins list with **Fork to customise**, workspace types with the bounded structural editor (§7 Q2) — rename/describe, archive, and per-section data-contract editing with move-up/down; linked from the main Settings page. 10 DB probes (built-ins globally readable + uneditable, fork clones sections/leaves source canonical/admin-gated/cross-workspace-isolated, scratch create + viewer-denied), 4 types unit tests, 1 E2E baseline. Gates `pnpm turbo lint typecheck test build` 38/38, DB 84/84 (run against a local PG16 — CI uses PG17), E2E 36/36, drift in sync (17 files) | Scope held to G0.1/G0.2 (the M5-critical path — generation needs the type+section schema). **G0.3** approval-role assignment (Phase-3 review config — not generation-blocking), **G0.4** Brand Profiles, **G0.5** Quality Standards, **G0.6** Product Briefs are the next G0 slices. Archive (not hard delete) is the type lifecycle per §3.8 (archived blocks new docs, existing untouched); a referential delete guard arrives with `documents` (0005/G3). 0017 to apply to cloud dev+prod post-merge. Quality-override editing per section deferred (jsonb passes through; no editor yet) |
| 2026-06-15 | F8.7 — workspace deletion (Danger Zone) | Closed the F8.7 task. The soft-delete columns + owner-only `request_workspace_deletion()`/`cancel_workspace_deletion()` RPCs already shipped in 0002; this adds the **destruction + restore** halves. Migration **0016** (both copies + README row): `purge_deleted_workspaces()` — the one sanctioned hard delete, service-role only (EXECUTE revoked from anon/authenticated). It runs under `session_replication_role = replica` to neutralise the immutability/archive/owner-row guards that otherwise block the delete — but since replica role **also disables FK enforcement** (so ON DELETE CASCADE would silently orphan every child), it deletes each workspace-owned row **explicitly**: a dynamic sweep over every `public` table carrying `workspace_id` (new tables covered automatically) plus the lone parent-scoped table `notification_preferences` (via `workspace_member_id`), then the workspace. `get_pending_workspace_deletion()` — definer read so a soft-deleted (RLS-hidden) tenant still surfaces its restore affordance to members. `@arther/db`: `requestWorkspaceDeletion`/`cancelWorkspaceDeletion`/`getPendingWorkspaceDeletion`. App `/settings`: owner-only **Danger Zone** (type-the-slug-to-confirm + browser confirm → soft delete) and, because the active workspace vanishes from RLS once pending, a **restore banner** (the 0016 lookup resurfaces it; owner restores in-window, members see the countdown). New DS `danger` Button variant (safety-warn tokens). 11 DB probes (owner-only request/cancel, tenant-hidden-on-request, definer restore lookup, replica-mode purge completeness incl. immutable child + members, in-grace survives, service-role-only); gates 38/38, E2E 35/35, drift in sync (16 files) | **Design correction:** the data model / migration comments describe the purge as a plain replica-mode `DELETE FROM workspaces` relying on cascade — that orphans children (replica disables FK actions). 0016 does explicit per-table deletes instead; the carve-out comment intent is preserved. The purge **cron** (`purge-deleted-workspaces`, daily) is noted in `packages/jobs` for Phase 2 Trigger.dev wiring (or Supabase pg_cron meanwhile) — the SQL function is complete and probe-covered now. 0016 to apply to cloud dev+prod post-merge. Danger Zone is data-bearing so it stays DB-probe-covered (no provisioned-E2E env yet), per the settings-spec precedent. **Remaining Phase-1 exit gate: F8.5 (input-validation sweep), F8.6 (single-handler audit); F8.4 (PITR) still blocked on a paid plan** |
| 2026-06-15 | Routine de-dup cleanup + autonomous-routine protocol | Diagnosed the hourly "next unblocked task" routine duplicating work: each run is a fresh clone of `main` with no memory, and the "mark progress when you open a PR" note lived only on the **unmerged** feature branch — so every hour a run re-read an unchanged `main`, re-picked **G0.1**, and opened a new PR on a fresh random branch (no open-PR check, no branch collision to surface it). Result: **7 open PRs for G0.1** (+ #27 redoing already-merged F8.5/F8.6). Cleanup: selected **#34** (G0.1/G0.2 — green CI, ADR-012 `@arther/types` one-source, reuses `workspace.manage`) as canonical and merged it; closed the six G0.1 duplicates #28/#29/#30/#33/#35/#36 (#28/#35/#36 were also red on the DB/E2E probes) + #27, each with a reason. Fix: new **[CLAUDE.md → Autonomous task routine]** (list open PRs/`claude/*` branches *before* picking; task-scoped branch; enable auto-merge so `main` actually advances; opening a PR is not a done-marker — merging is) + a §0 cross-reference here. | #31 (G0.4 Brand Profiles) and #32 (G0.6 Product Briefs) left open — independent G0 tasks, not duplicates. Auto-merge-on-green still needs the **owner** to turn on the repo's "Allow auto-merge" setting + a branch-protection rule requiring the three CI checks (+ owner review); the routine prompt must also call enable-auto-merge on each PR it opens |
| 2026-06-15 | G0.3 — Approval roles config (Phase 2 G0) | The last open G0 axis with no in-flight PR (open: #38 G0.5; merged: G0.1/G0.2 #34, G0.4 #31, G0.6 #32). Like the other G0 management slices it's a **pure layer over the 0004 tables — no migration** (`document_type_approval_roles` + `approval_role_assignments` + member-read/admin-write RLS already shipped). The named reviewers a Document Type requires before publish; configured now, consumed by the Phase 3 AND-logic review machine (data model §approvals). **`@arther/types` `approval-role.ts`** (ADR-012, one source): `approvalRoleLabelSchema` (F8.5-bounded) + `approvalRoleFormSchema` (label + required flag); new `ApprovalRoleId`/`ApprovalRoleAssignmentId` brands + id schemas. **`@arther/db` `approval-roles.ts`**: `listApprovalRoles` (roles in display order, each with its member assignments via a nested select), `create`/`update`/`delete` role, and `assign`/`unassign` member (PostgREST upsert on the `(role_id, workspace_member_id)` unique so re-assigns are idempotent). **App**: an **Approval roles** section on a workspace Document Type's detail editor (`/settings/document-types?type=`) — add/edit (label + required toggle)/delete a role, and assign/unassign workspace members per role; admin-gated (`canDo('workspace.manage')` + 0004 RLS defence-in-depth). Built-ins stay read-only (the `dtar_write` `workspace_id is not null` guard means you fork first). 6 DB probes (`tests/db/approval-roles.test.ts` — owner write / member read-only / built-in roles read-only / assignment admin-gated + unique / stranger isolation), 5 types unit tests, 1 E2E baseline. Gates `pnpm turbo lint typecheck test build` 38/38, DB 102/102 (local PG16 — CI uses PG17), E2E 42/42, drift in sync (no migration) | Roles live in the Document Type detail editor (built-ins forked first), not a standalone route — the E2E is the host surface's unprovisioned baseline (the role/assignment CRUD is DB-probe-covered until the provisioned-E2E env lands, per the G0.1/G0.4 precedent). Phase 3 consumes these roles for `approval_records` AND-logic; the assignment→reviewer wiring is C0–C2. **G0 epic now complete pending #38 (G0.5) merge.** |
| 2026-06-15 | G0.5 — Document Quality Standards (Phase 2 G0, independent axis) | Took the next unblocked G0 task with no open PR/branch (no open PRs existed at run start; G0.1/G0.4/G0.6 already merged in §10). Like G0.4/G0.6, a **pure management layer over the 0004 tables — no migration**: the `document_quality_standards` table + member-read/admin-write RLS already shipped. The Quality Standard is the generator's editorial-discipline input (generator spec §3.5/§4.4 — section length limits, required structural elements, voice/mood rules, conditions metadata), deliberately separate from Brand Profiles (presentation vs. discipline, different owners). **`@arther/types`** `quality-standard.ts` (ADR-012 one source): `QualityConstraint` shape + scope enum (`global`/`section`/`block_type`), pure pipe-delimited `parseQualityConstraints`/`formatQualityConstraints` round-trip (skips malformed lines, F8.5-bounded per field, count-capped) + `qualityStandardFormSchema`; new `QualityStandardId` brand + schema (11→ now 10 unit tests). **`@arther/db`** `quality-standards.ts`: list (with per-standard "N document types reference this" counts), get, create, update, and a delete that surfaces the `document_types.quality_standard_id` FK referenced-block as a typed `blocked: 'referenced'` (the "can't delete while referenced" rule, DB-enforced — no `archived_at` on this table, so it's a real delete) instead of a 500. **App** `/settings/quality-standards` (list + create + referenced-guarded delete) and `[id]` editor, owner/admin-gated (`canDo('workspace.manage')` + 0004 RLS); linked from Settings; malformed ids validated at the boundary (F8.5). 10 types unit tests, 5 DB RLS probes (`tests/db/quality-standards.test.ts` — admin write / member+viewer read-only / **referenced-delete guard** / stranger isolation; **green in CI's Postgres container** this run), 2 E2E baselines. Gates `pnpm turbo lint typecheck test build` 38/38, E2E 43/43, drift in sync (13 files, no migration). PR **#38**, auto-merge requested. | Constraints edited as pipe-delimited `scope \| target \| rule \| description` lines (a structured per-constraint editor is a later polish; the parser is the contract). No default/archive lifecycle (the table has neither) — simpler than Brand Profiles. DB probes authored + **run green in CI** (no Docker this session). **Remaining G0: G0.3 approval-role assignments** (Phase-3 review config, configured now). Auto-merge still gated on the owner's repo "Allow auto-merge" setting + branch protection |
| 2026-06-15 | Auto-merge diagnosis + routine fix | Diagnosed why "Allow auto-merge" being on wasn't landing PRs. Two causes: (1) **#38 (G0.5) was stuck `dirty`** — a merge conflict it picked up when #39 (G0.3) merged and changed the same shared registry lines (`packages/types/src/ids.ts`/`index.ts`, `packages/db/src/index.ts`, `IMPLEMENTATION_PLAN.md §10`); resolved it (union-of-additions, both log rows kept), verified `pnpm turbo lint typecheck test build` 38/38 + drift in sync, pushed → #38 went `clean` and merged. (2) **No branch-protection required-review rule on `main`**, so a green PR is `clean` (immediately mergeable) and GitHub refuses to arm auto-merge ("merge directly") — and while checks run it's `unstable` and also refuses. There is no `blocked` window for auto-merge to engage, so the checkbox alone is inert; prior PRs landed via manual mobile merges, never auto-merge. Updated **CLAUDE.md → Autonomous task routine**: enable auto-merge only after CI is green (the `blocked` window), retry on `unstable`, and treat a `clean`-status refusal as the signal the required-review rule is missing. | **Owner action required:** add a `main` branch-protection rule requiring an approving review + the three CI checks — that creates the pending requirement auto-merge waits on. Until then the routine cannot self-land work (manual merge only). The recurring §10/registry conflicts between parallel G0 PRs remain a known papercut (per-entry log files would remove them) — deferred. |
| 2026-06-15 | G1.3 — Tool-use block schema layer (Phase 2 G1) | First **G1** slice and the next critical-path step (`G0▶G1▶G2▶G3`): G0 done (G0.5 open as #38), G1.1 `ai-gateway` already shipped (F7), G1.2 Trigger.dev / G1.4 progress-streaming both gated on provider/table wiring — **G1.3** (Zod block schemas → Claude tool-use JSON contract) is buildable on `main`, no migration, no open PR/branch touches it. New **`@arther/types` `block-content.ts`** authored against **`zod/v4`** (the `@arther/ai-gateway` SDK-helper engine; the `@arther/spec-import` posture — rest of `@arther/types` stays classic v3, only `TEXT_LIMITS` constants cross). **`blockContentSchema`** — discriminated union over **all 20 block types'** `content` payloads (Visual Block Editor spec §4.4–4.10): rich text (marks with colour guarded to text_color/highlight, inline spec tokens, links), `DegradationConfig`, structural/prose/data/safety/media/container/reuse; strict objects (H1 heading + stray divider fields rejected); a parity test pins the union to `BLOCK_TYPES`. **Container child rules (§4.11)** as data (`PERMITTED_CHILD_BLOCK_TYPES` + `canContain`/`isContainerBlockType`) and enforced *in* the union — no container nests Accordion/Step Wizard/Snippet. **`generatedSectionSchema`** — the leaner section-scoped contract Claude emits (G2.2), with an **AI spec token carrying only `field_id`** + product/component scope so the model can't mint a `field_version_id`/`display_value` (G2.5 zero-hallucination, mirroring import; app resolves at commit). **`generationToolJsonSchema()`** renders it to the tool-use `input_schema` (zod/v4 `toJSONSchema`). 14 new unit tests (91 total in `@arther/types`); gates `pnpm turbo lint typecheck test build` 38/38, drift in sync (no migration). PR #40, auto-merge to set once CI green. | Authored in zod/v4 (not v3) because the contract must feed the gateway's SDK helper — kept to the one new module; existing v3 schemas untouched. No `test:db` (no SQL) and no new E2E (pure schema layer, no app surface — both apps build clean), per the G1.1/F8.2 non-surface-package precedent. The nested container tree models the spec's logical shape; the physical persistence mapping (nested JSONB vs `blocks.parent_block_id` rows) is settled in G3. G2 wires `generatedSectionSchema` into the gateway per-section call. |
| 2026-06-15 | G3 — Block model & persistence (Phase 2, M5 critical path) | Next on `G0▶G1▶G2▶G3` and the foundation both G4 (editor) and G6 (tracking) sit on. No open PR/branch and **no migration** — 0005 (`documents`/`document_revisions`/`blocks`/the three reference tables/`generation_runs(_sections)`) already shipped *including the G3.4 archive-guard extension* (`guard_component_hard_delete`/`guard_field_hard_delete` already check `block_spec_references`). This is the `@arther/db` repository + `@arther/types` contract + DB probes over it. **`@arther/types`**: 5 new branded ids (`DocumentRevisionId`/`BlockId`/`BlockSpecReferenceId`/`BlockBriefReferenceId`/`PlaceholderBriefReferenceId`) + new `document.ts` (classic v3) — persistence enums mirroring the 0005 CHECKs (`DOCUMENT_STATES`, `BLOCK_SOURCES` — the full 6-value set incl. `manual`/`snippet`, broader than the generator's 4-value subset, `BLOCK_REFERENCE_TYPES`), `slugifyTitle` + `documentSlugSchema`, `documentCreateSchema`, and the Zod-gated reference write-inputs (F8.5-bounded). **`@arther/db` `documents.ts`**: `createDocument` (document → first draft revision → wire `current_revision_id`, with one suffixed-slug retry on the per-product unique), `getDocument`/`listDocumentsForProduct`/`setDocumentArchived`, `insertBlocks`/`loadRevisionBlocks`/`updateBlock`/`deleteBlock` (every write validated through `blockContentSchema` — its `type` must equal the row `type`), `addSpecReference`/`addBriefReference`/`addPlaceholderReference`, `loadDocumentTree` (the round-trip read), and `listStaleSpecReferences` (the `field_version_id <> spec_fields.current_version_id` join, embed + TS diff — the foundational read G6.1 builds on). 9 types unit tests (119 total in `@arther/types`) + `tests/db/documents-blocks.test.ts` (12 probes: revision wiring, ordered block-tree round-trip with nested container content intact, `text_search` FTS projection, brief/placeholder refs incl. one-per-block uniqueness, staleness join empty-then-affected across a version bump, **G3.4 field + component delete guards**, viewer-read/editor-write RLS, stranger isolation, generation_runs service-role-only). Gates `pnpm turbo lint typecheck test build` **38/38**, drift in sync (no migration). | **Persistence decision (the mapping G1.3 deferred to G3):** a top-level block is one `blocks` row ordered by `display_order`; container interiors (accordion sections/steps, safety-block children) persist **inline in `content`** per `blockContentSchema` — forced for Accordion/Step Wizard (their section→children shape is two logical levels, unmappable to the one-level `blocks.parent_block_id`) and applied uniformly. **`parent_block_id` is therefore reserved (null this phase)** — a documented deviation from the 0005 "one-level containers" comment, superseded by the inline-content model the G1.3 union established. No app surface (editor is G4) so no E2E, per the G1.1/G1.3 non-surface-package precedent. DB probe authored + typechecks; **run in CI's Postgres service container** (docker daemon unavailable this session). The all-or-nothing generation commit is the G2.6 RPC; `createDocument`'s 3-step PostgREST sequence is acceptable for an authoring action (a failed step 3 leaves a valid null `current_revision_id`). |
| 2026-06-15 | G1.4 — Generation progress streaming (Phase 2 G1) | Closes G1's buildable remainder and the run-state layer G2 writes into / G2.8 reads. No migration — 0005 `generation_runs`/`generation_run_sections` already shipped (per-section status, the resume record for partial-failure + section-level retry, per-run token/cost accounting; **service-role writes only**, members read — no authenticated write policy, so a run can't be forged from a client). **`@arther/types`** `generation-run.ts` (ADR-012 one source): 2 branded ids (`GenerationRunId`/`GenerationRunSectionId`); the 0005-mirrored enums (`GENERATION_RUN_KINDS`, `GENERATION_RUN_STATUSES`, `GENERATION_SECTION_STATUSES`) with `isTerminal{Run,Section}Status`; `generationRunCreateSchema`/`generationRunSectionInputSchema` (write-boundary, F8.5); and **`summarizeRunProgress`** — the pure poll/Realtime read model (per-status counts, terminal `completed`, rounded `percentComplete`, `done`) so the status UI and editor agree without a round-trip. **`@arther/db`** `generation-runs.ts`: writes (`createGenerationRun` + section scaffold, `setGenerationRunStatus`, `setGenerationSectionStatus`) go through the **service client wrapped in `scopedServiceQuery`** (guardrail 1 — first real service-role write path; every update also pins `workspace_id` so a service path can't cross tenants; `started_at`/`completed_at` auto-stamped on running/terminal); reads (`getGenerationRun` → run+sections in order, `listGenerationRunsForProduct`) go through the user client under RLS. 8 types unit tests (125 total in `@arther/types`) + `tests/db/generation-runs.test.ts` (8 probes — member read of a service-created run+sections, **authenticated client can't forge/insert/update a run or section** = the no-client-write guarantee, service-role status/token/produced-block transitions persist, run accounting, stranger isolation). Gates `pnpm turbo lint typecheck test build` **38/38**, drift in sync (no migration). | **Live Realtime subscription is deferred to G2.8** (the generation UX surface) — this slice ships the persisted read model + the pure `summarizeRunProgress` the client polls; wiring `supabase.channel(...)` is app-side when there's a run to watch. No app surface yet (G2.8/G4) so no E2E, per the G1.1/G1.3/G3 non-surface-package precedent. **G1.2 (Trigger.dev durable runtime) remains** the open G1 item — gated on Trigger.dev creds; until then generation runs inline in the server action (the F7 import precedent, `maxDuration` 300) writing these same rows. **Next on the critical path: G2 — the AI Document Generator** (wire `generatedSectionSchema` into the gateway per section → zero-hallucination validate → atomic `commit_generation()` RPC into the G3 tables; mockable gateway until `ANTHROPIC_API_KEY` lands). |
| 2026-06-15 | G2.1 — Generation pre-flight completeness (Phase 2 G2, first generator slice) | The author-facing entry to generation (generator spec §5.1): pick a Document Type and see how complete this product's spec inputs are before confirming. No migration. **`@arther/types`** `generation-preflight.ts` (pure, ADR-012): `computeGenerationReadiness(sections, fields)` maps a product's fields onto a Document Type's sections **by category** — exactly as the generator will at G2.2 — reporting per-section populated/empty, **required-but-empty** (these don't block; they become G2.7 placeholders that can't publish — so it's a *report*, not a gate), categories a section asks for with no field, and fields in no section (won't be injected); 6 unit tests. **`@arther/db`** `listPreflightFields` assembles the product's own + every attached component's fields (with `required` + populated = `value is not null`) under RLS. **App** `/specs/generate` — full-canvas pre-flight in Specs mode (top bar, no rail, like Import): Document-Type picker (archived excluded, §3.8), the per-section completeness report with required-empty warnings + brand-profile selection, and a **"Generate draft" confirm** that authorizes `canDo('doc.generate')` then **queues a `generation_run`** (G1.4) via the service client with a section scaffold mirroring the type — landing on an honest "queued; the durable processor (G2.2) runs it next" view (`summarizeRunProgress`). "Generate document" entry added to the product header. 6 types unit tests + 2 E2E (unprovisioned baseline + top-bar). Gates `pnpm turbo lint typecheck test build` **38/38**, E2E **46/46**, drift in sync (no migration). | The confirm **creates a queued run but nothing processes it yet** — the durable generate-validate-commit pipeline (G2.2→G2.6) is the next slice; the queued run is the honest handoff (and exercises G1.4 end-to-end). No new DB probe: `listPreflightFields` is a read over existing RLS (already probe-covered by the spec/brief suites) and the run-creation path is covered by the G1.4 `generation-runs` probes; the mapping logic is unit-tested. Brand selection is a plain `<select>` (no DS Select component yet); generation degrades honestly without Supabase/`ANTHROPIC_API_KEY` (the E2E baseline). **Next: G2.2/G2.5/G2.6 — section-scoped generate → zero-hallucination validate → atomic `commit_generation()` into the G3 tables (mockable gateway until the key lands).** |
| 2026-06-15 | G2.6 + G2.5 — generation commit RPC (the deterministic generator core) | The atomic spine of generation, gateway-independent and fully DB-probe-tested. Migration **0018** (both copies + README row) `commit_generation(run_id, title, blocks)`: in ONE transaction turns a queued/running `generation_run` into a **Draft** — creates the `documents` row + first draft `document_revisions` (wires `current_revision_id`), inserts the produced `blocks` tree in array order, and writes `block_spec_references` **resolving each cited `field_id` to the field's CURRENT `field_version_id`** in the DB. **Zero-hallucination (invariant 6, G2.5):** a reference to an unknown / cross-workspace field, or to a field with **no value**, is `raise`d and the whole commit rolls back — a partial document can never persist (G2.6 all-or-nothing). **Idempotent retry guard (G8.1):** a run with `document_id` already set can't re-commit. **Service-role only** (like `generation_runs` writes, G1.4): EXECUTE revoked from `public`/`anon`/`authenticated`, granted to `service_role` (BYPASSRLS); invoker rights, every write scoped to the run's workspace; the app authorizes `canDo('doc.generate')` before the service path runs. **`@arther/db`** `commitGeneration(service, scope, {runId, title, blocks})` wraps the RPC under `scopedServiceQuery`, folding P0001 rule errors to `DbRuleError`. `tests/db/generation-commit.test.ts` — 5 probes: atomic success (document/revision/ordered block tree/resolved ref/run flipped to `succeeded`+`document_id`), **unknown-field reject + rollback** (run stays `queued`, no document), valueless-field reject, double-commit guard, **service-role-only** (authenticated EXECUTE denied). Gates `pnpm turbo lint typecheck test build` **38/38**, drift in sync (18 files). | Commits the **happy path** (run → `succeeded`); **partial-commit** (some sections failed → Draft + error blocks, status `partial`) and **section-level retry** are the next refinement (G2.6 cont.). The RPC trusts the app to resolve inline content tokens (field_version_id + display_value) before commit — the **block_spec_references are the authoritative staleness anchor** and are DB-resolved here; a tiny TOCTOU window between app token-resolution and the RPC is acceptable (G6 staleness catches drift). DB probe authored + typechecks; **runs in CI's Postgres container** (docker daemon unavailable this session). 0018 to apply to cloud dev+prod post-merge. **Next: G2.2 — wire the per-section gateway call (`generatedSectionSchema` → resolve field_id tokens → assemble blocks) feeding `commitGeneration`; mockable until `ANTHROPIC_API_KEY`.** |
| 2026-06-15 | G2.2/G2.3/G2.5 — generation assembly + zero-hallucination resolution (deterministic, gateway-independent) | The transform between the model and the commit RPC, pure and exhaustively unit-tested. **`@arther/types`** `generation-assembly.ts`: `resolveGeneratedSection(section, resolve)` turns a model-authored `GeneratedSection` (whose inline tokens carry only a `field_id`, G1.3) into **commit-ready `GenerationCommitBlock[]`** — walking every block's rich text (and safety-block children, and spec_table rows), resolving each token through an **injected `FieldResolver`** to the current `field_version_id` + formatted `display_value` + unit (the model never mints a value, mirroring import), building the block's `specRefs`, and projecting `text_content` for FTS. **Zero-hallucination (invariant 6, G2.5):** a token whose `field_id` doesn't resolve is **dropped and reported in `unresolvedFieldIds`** so the caller rejects the section; when empty the blocks are fully grounded for the 0018 `commit_generation` RPC (which independently re-resolves to current at the atomic commit). Pure (resolver injected) → no DB/gateway needed to test. **`GenerationCommitBlock` moved to `@arther/types`** as the one commit contract (ADR-012); `@arther/db` re-imports + re-exports it (public API unchanged). One **schema relaxation**: `inlineSpecTokenNodeSchema.unit_id` is now **nullable** (unitless fields — booleans, enums — the display value carries the text). 5 assembly unit tests + the existing block-content suite still green (132 in `@arther/types`). Gates `pnpm turbo lint typecheck test build` **38/38**, drift in sync (no migration). | Pure logic package addition — no app surface (so no E2E) and no DB probe (the commit path is covered by the 0018 `generation-commit` probes; the relaxed token schema by the block-content suite), per the non-surface-package precedent. An unresolved token is **dropped** from the assembled prose (the section is meant to be rejected/regenerated when `unresolvedFieldIds` is non-empty) rather than rendered as a broken token. **Remaining G2.2: the orchestrator** — load a queued run's sections + spec graph, call `gateway.structured({schema: generatedSectionSchema, …})` per section (mockable until `ANTHROPIC_API_KEY`), feed `resolveGeneratedSection` + the `FieldResolver` (built from the product's current field versions via `formatFieldValue`), drive `setGenerationSectionStatus`, and `commitGeneration` — wired to the pre-flight confirm (inline in the action, F7 precedent, until G1.2 Trigger.dev). |
| 2026-06-15 | G2.2 — generation orchestrator (the section-loop core) | The orchestration that strings the merged pieces (gateway G1.1, schema G1.3, assembler G2.2, commit G2.6) into a run — in **`@arther/ai-gateway`** per its own design note ("Phase 2 generation adds its call shapes here, not a second client"); the package gains a one-way `@arther/types` dep (no cycle). `generation.ts`: **`buildSectionPrompt`** (the slot-filler injection §5.1 — lists citable fields by id, forbids writing values as plain text, injects brief/brand-voice/quality); **`buildFieldResolver`** (entries → the `FieldResolver` the assembler calls); **`generateSection`** (force the model to return a `GeneratedSection` via the one tool → `resolveGeneratedSection` → **zero-hallucination gate**: a section whose tokens don't all resolve yields no blocks and is reported, so the run goes `partial` and the section is retryable); **`generateDocument`** (run sections in order, prepend a `section_header` to each success, drive injected per-section status hooks, return `succeeded`/`partial`/`failed` + the accumulated blocks). Everything injected (gateway, resolver, hooks) → exhaustively unit-tested with a **mock gateway** (no real model/DB): 7 tests covering resolver lookup, prompt content, section success/zero-hallucination-fail/gateway-throw, and a 2-section run going `partial` with the header prepended only to the success. Gates `pnpm turbo lint typecheck test build` **38/38** (+5 existing gateway tests still green), drift in sync (no migration). | Pure/injected logic — **no app surface** (no E2E) and **no DB probe** (the commit path is covered by the 0018 probes; the orchestration by the mock-gateway unit tests), per the non-surface-package precedent. **The last wiring step (the M5 finish line):** the pre-flight **"Generate draft"** confirm action loads the run's spec graph + Document-Type sections (a `@arther/db` context reader — fields with `current_version_id` + `formatFieldValue` display), builds the resolver + `SectionPlan`s, instantiates the gateway (`createAiGateway`, degrades honestly without `ANTHROPIC_API_KEY`), runs `generateDocument` with hooks that persist `generation_run_sections` status, then `commitGeneration` → Draft. That action runs inline (`maxDuration` 300, F7 precedent) until G1.2 Trigger.dev. With the key set (owner), spec + type → a grounded Draft — M5 acceptance. |
| 2026-06-15 | G2.2 wired — generation runs end-to-end (M5 acceptance path) | **The loop closes.** The pre-flight "Generate draft" confirm now *runs* generation inline (owner set `ANTHROPIC_API_KEY` in Vercel). New **`@arther/db` `loadGenerationFields`** — the product + attached-component spec graph with `current_version_id` + typed value + unit (the resolver/prompt source, RLS). **App `createGenerationRunAction`** rewritten: authorize `doc.generate` → create the run (service, G1.4) → `createAiGateway({apiKey})`; **provisioned** → run inline, **unprovisioned** → leave `queued` and degrade honestly. Inline run (`runGenerationInline`): build the **`FieldResolver`** (only fields with a current version are citable; `formatFieldValue` + unit symbol for display; product/component scope supplied by the resolver, never the model) + per-section **`SectionPlan`**s (each run-section matched to its `DocumentTypeSection` for the category→field filter; brief fragments + brand voice/tone injected), then **`generateDocument`** with hooks that stream per-section status into `generation_run_sections` (G1.4), then **`commitGeneration`** (0018) atomically → Draft, run flipped `succeeded`/`partial`, token usage accounted via the gateway's `onUsage`. The `?run=` view now reflects the real outcome (Draft created / partial / failed / queued + per-section statuses; "the block editor (G4) opens it next"). **Contract correctness:** the inline spec token's `component_id` is now **nullable** (product-owned fields) and `resolveGeneratedSection` takes the token's product/component scope from the **resolver** (authoritative), so the model cites only `field_id` and can't misattribute scope (extends zero-hallucination). Gates `pnpm turbo lint typecheck test build` **38/38**, E2E **46/46** (pre-flight + run-view baselines hold), drift in sync (no migration). | **M5 — Generate — is structurally complete:** spec + type → a stored, grounded Draft, zero-hallucination-validated and atomically committed. End-to-end with a real model is the **owner's to verify in prod** (the key is set; this session has no `ANTHROPIC_API_KEY`/docker, so the real call is covered by the mock-gateway unit tests + the 0018 commit probes + the honest-degrade E2E). Token accounting is per-run totals (not yet per-section split). Quality-standard constraints aren't injected into the prompt yet (brief + brand are) — a thin follow-up. Component briefs aren't walked into the prompt (product brief is) — deferred with the graph-brief assembly. **Next: G3 documents/blocks UI is done at the data layer; G4 (the three-panel block editor) opens these Drafts — the M6 milestone.** |
| 2026-06-15 | G4.4 — shared block renderer + read-only document view (M6 kick-off) | The generated Drafts are now **viewable**. Fleshed out the **`@arther/block-renderer`** stub into the one read-only renderer (ADR-008/architecture §4 — editor preview, portal SSR, PDF all render through it): `RichText` (marks → semantic tags, colour/highlight inline, inline **spec tokens render their resolved `display_value`** as non-editable chips, links) + `BlockRenderer` — an exhaustive switch over **all 20 `BlockContent` types**: prose (section_header/heading/paragraph/callout/code_block), safety blocks + their children, and containers (accordion → `<details>`, step_wizard → ordered steps) render fully; data/media (spec_table/chart/toc/image variants/snippet) render structurally with a labelled placeholder until live-value resolution lands. New React package (mirrors `@arther/ui`: React peer dep, `jsx: react-jsx`); 6 unit tests via `renderToStaticMarkup` (real render: prose+token, marks, callout variant, safety children, spec_table placeholder w/ row count, accordion `<details>`). **App** `/documents/[id]` — read-only doc view loading the working revision's block tree (G3 `loadDocumentTree`) and rendering it; malformed id → "not found" not a 500 (F8.5); wired into `transpilePackages` + app dep. The generate `?run=` view now shows an **"Open draft"** link to the committed document. 2 E2E (unprovisioned baseline + top-bar). Gates `pnpm turbo lint typecheck test build` **39/39** (new package = +1 task), E2E **48/48**, drift in sync (no migration). | First G4/M6 slice — chose the **read-only renderer + view** so M5's output is immediately visible end-to-end (generate → Open draft → read the grounded prose with inline values). The **three-panel editor shell (G4.1)**, **TipTap editing model (G4.3)**, **per-type property editors (G4.2)**, and **full data/media rendering** (spec_table/chart need live field-value resolution; the renderer stubs them today) are the next G4 slices. No DB probe (pure render package + a read-only surface over G3's probe-covered `loadDocumentTree`); rendering is `renderToStaticMarkup`-tested. The portal will render through this same package at C6. |
| 2026-06-15 | G4.1 — three-panel block editor shell | The editor IA (Handoff 02/03) mapped onto the app shell's regions — **Outline (navigator) · canvas (content) · Properties (inspector)**, reusing the existing `AppShell` 5-region frame. New **`@arther/block-renderer` `buildOutline`** (pure: section headers + headings → a levelled, navigable outline; 3 unit tests). **App `/documents/[id]/edit`** — server loads the working revision's block tree (G3) → client **`DocumentEditor`**: block **selection** drives the Properties inspector (type/source today; per-type editors are G4.2); the **Outline** lists sections/headings and scroll-selects them; panels **toggle** via the toolbar buttons and the Handoff §6 keybindings (**⌥⌘\\** outline, **⌥⌘⇧\\** properties, **⌘\\** focus); the canvas renders **read-only through the shared block-renderer** (one block per selectable wrapper). "Edit" link added to the read-only document view. 3 `buildOutline` tests + 2 E2E (unprovisioned baseline + top-bar). Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | The frame is real; **in-place editing is intentionally absent** — the canvas is read-only until the **TipTap/ProseMirror model (G4.3)** lands (spec tokens as atomic inline nodes) and **per-type property editors (G4.2)** fill the inspector. Selection/outline/toggle interactivity is client-only (covered by the pure `buildOutline` unit tests + typecheck + the honest-degrade E2E; provisioned-data interaction E2E awaits the env). Panel toggles are local React state (not persisted); the canvas renders each block in its own selectable wrapper (per-block, not the whole-doc `BlockRenderer`) so selection has a target. **Next G4: G4.3 editing model, G4.2 property editors, G4.5 edit/preview modes, then G5 auto-save.** |
| 2026-06-15 | G4.3 — rich-text editing model (TipTap; spec tokens as atom nodes) | The canvas is editable. **`@arther/block-renderer` `prosemirror.ts`** — the pure `RichTextContent ⇄ ProseMirror` conversion (ADR-013, the editor↔persisted seam): a block's inline content ⇄ a one-paragraph PM doc (alignment as an attr), text marks ⇄ PM marks (bold/italic/underline/strike/super/sub/code/textStyle-color/highlight), and **inline spec tokens ⇄ an atom node** carrying the resolved value attrs; 4 unit tests prove a **lossless round-trip** and that the token attrs (the grounded value) survive verbatim. **App** (TipTap pinned, plan §risk): `RichTextEditor` (client) — StarterKit (block extras off; one-paragraph inline editing) + underline/super/sub/text-style/color/highlight + a custom **`SpecToken` ProseMirror `Node` (`atom: true`, `contenteditable=false`)** so a value can be placed/deleted as a unit but **never hand-typed** (invariant 6 holds in the editor); loads via the conversion, reports `RichTextContent` on blur. `DocumentEditor` now holds block state — **paragraph/heading/callout render the live editor**, other types stay read-only; edits **persist on blur** via the new `updateBlockContentAction` (re-validates through `blockContentSchema`, recomputes the FTS projection, **editor-gated by the new `doc.write` authz action** + RLS), with a Saving/Saved indicator and the outline reflecting heading edits. 4 conversion unit tests; gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | **Live editing is prod-verified** — the conversion is exhaustively unit-tested (the deterministic core) but the TipTap React editor needs a browser (no jsdom editor test this session); `useEditor({ immediatelyRender: false })` for Next 15/React 19 SSR. Scoped to **paragraph/heading/callout content** editing; structural attrs (heading level, callout variant/title) + the other 17 block types' editors are **G4.2**; **links aren't authored** in v1 (the conversion loads existing ones but the editor exposes no link button); **block insert/delete/reorder** is G4.6; debounced **auto-save + offline queue** is **G5** (blur-save is the bridge). New `doc.write` action added to `@arther/authz` (editor-gated, defence-in-depth over the 0005 blocks RLS). TipTap adds ~66 deps (pinned `^2.10`). **Next G4: G4.2 property editors, G4.5 edit/preview, G4.6 cross-block ops; then G5.** |
| 2026-06-15 | G4.2 (first slice) — block property editors in the inspector | The editor's right panel now **edits structure** (G4.3 edits the rich-text *content* in the canvas; this edits the block's structural attrs). New **`BlockProperties`** client component, mounted `key`ed by block id (text state resets on selection change): **section_header** → title, **heading** → level (H2/H3), **callout** → style (info/tip/important) + title, **paragraph** → alignment — each producing a new `BlockContent` committed through the same editor-gated `updateBlockContentAction` (re-validated `blockContentSchema`, FTS recomputed, `doc.write` + RLS). Selects commit on change; text commits on blur (G5 debounces). Wired into `DocumentEditor`'s inspector beside the type/source readout. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | First G4.2 slice — the **generated prose family** (section/heading/callout/paragraph), the structural attrs of the blocks generation actually produces. The other 16 types' editors (spec_table column config, chart config, image url/alt, accordion/wizard sections, …) are subsequent slices. UI-only (no new unit test — the change is `{...content, attr}` spreads + select/text controls; covered by typecheck/build + the editor's E2E baseline; live property-editing is prod-verified like G4.3). **Next G4: more block-type property editors, G4.6 cross-block ops (insert/delete/reorder), G4.5 edit/preview modes; then G5 auto-save.** |
| 2026-06-15 | G4.6 (first slice) — block insert / delete / reorder | Authoring, not just editing: you can now **add, remove, and reorder** blocks. New **`@arther/db` `reorderBlocks`** (rewrites `display_order` from an ordered id list, editor-gated by RLS). Three editor actions (a shared `authorizeDocWrite` `doc.write` guard): **`addBlockAfterAction`** (insert an empty paragraph after the selected block — or at the end — via G3 `insertBlocks`, then `reorderBlocks` to position it; returns the new block + the new order), **`deleteBlockAction`** (cascades its `block_spec_references`), **`reorderBlocksAction`**. `DocumentEditor` wiring: a **"+ Paragraph"** toolbar action (inserts after the selection, selects it for immediate typing) and **Move up / Move down / Delete** in the inspector for the selected block, all with optimistic local state + the Saving/Saved indicator. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | First G4.6 slice — single-block insert (paragraph)/delete/move; **multi-select, drag-reorder, copy/paste across docs** (the rest of G4.6) are follow-ups, as is **inserting non-paragraph block types** (needs a block-type picker, pairs with the remaining G4.2 editors). No new DB probe — `reorderBlocks`/`deleteBlock` are editor-gated writes over the G3-probe-covered `blocks` RLS (delete-cascade of references is the 0005 FK); UI/ops are typecheck/build-verified, live-verified in prod. Reorder rewrites the full `display_order` sequence (fine for v1 doc sizes; a fractional-order or RPC optimization is a later perf item). **Next G4: G4.5 edit/preview modes, remaining property editors + block-type insert; then G5 debounced auto-save (replacing the per-op saves).** |
| 2026-06-15 | G5 (first slice) — auto-save + offline queue + connectivity (M6) | The editor's save path is hardened into the connectivity model. New **`@arther/types` `SaveQueue`** — the pure, timer-free core: latest-wins per-block coalescing, first-edit-order draining, a value re-edited mid-save kept (not dropped), failed/offline saves retained, and a derived `idle/pending/saving/offline/error` status; **6 unit tests** (139 total in `@arther/types`). App **`useSaveQueue`** hook wraps it with a **600ms debounce**, the `online`/`offline` events (drains on reconnect), and a single-flight flush. `DocumentEditor` now routes **content edits through the queue** (G5.1/G5.2) instead of an immediate per-blur save, shows an **always-on Connected / Saving / Offline indicator** (G5.3), and **blocks the structural ops** (+Paragraph / Move / Delete) while offline (G5.5). Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | First G5 slice — debounced auto-save + an in-memory offline queue that drains on reconnect, the connectivity indicator, and offline-blocked structural ops. **Deferred:** G5.4 reconnect **conflict resolution** (block-level keep-mine/use-server) + the G5.1 advisory **optimistic lock / "who's editing" banner** (need presence/version signals), cross-**reload** queue persistence (localStorage — the queue is in-memory this slice), and extending offline-blocking to generation/publish/invite on their own surfaces. The queue core is unit-tested; the hook's timer/online behavior is prod-verified (no jsdom timer test this session). **Next: G4.5 edit/preview modes, G5.4 conflicts, remaining G4.2/G4.6 (block-type insert, drag, multi-select); then the M7 staleness/dashboard track (G6).** |
| 2026-06-15 | G4.5 — edit / preview modes | An **Edit ↔ Preview** toggle in the editor toolbar: **Edit** keeps the three-panel editable canvas (TipTap prose, selection, +Paragraph, inspector ops); **Preview** drops the Outline/Properties panels and renders the working block tree **read-only through the shared `block-renderer`** in a clean reading column — "how it'll publish" (the same renderer the portal/PDF use at C6/C5). The toolbar collapses to just the mode toggle + Done in preview; the connectivity indicator stays (queued saves still flush). Preview reflects **live in-memory edits** (it reads the same `blocks` state), so toggling is an instant before/after. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | UI-only (no new unit test — a mode switch over the already-tested `BlockRenderer`; covered by typecheck/build + the editor E2E baseline; live-verified in prod). Preview hides the panels for a clean read; keeping the Outline navigable in preview would need the renderer to emit block anchors — a later polish. **Remaining M6: G5.4 reconnect conflicts + "who's editing" lock, full spec_table/chart live rendering, drag-reorder / multi-select / non-paragraph insert, find-replace (G4.7). Next milestone: M7 — Smart Spec Tracking (G6) — spec change → staleness → the owner's action dashboard (the `listStaleSpecReferences` join is seeded from G3).** |
| 2026-06-16 | G6.1 (first slice) — document staleness surface (M7 kick-off) | The headline "kept in sync" signal: a document now **shows when its spec values have drifted**. New **`@arther/db` `listStaleReferencesForDocument`** — the enriched staleness read for one document (`block_spec_references` whose anchored `field_version_id` ≠ the field's current version, joined to the field name/category), under RLS. New **`@arther/types` `summarizeStaleness`** (pure: dedupe changed fields + affected blocks; 2 unit tests, 135 total). Surfaced as a **"N spec values changed since generation (field names) — review" banner** on the **read-only document view** and the **editor**, with the editor **marking the affected blocks** (a warn-coloured left bar via `block_spec_references`→block id). Generate → edit a cited spec field → the document/editor flags exactly which values moved. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | First G6 slice — **read-only staleness detection + surfacing** (G6.1). No migration, no new DB probe: the staleness join is already probe-covered (G3 `documents-blocks` staleness probe) and the reader is an enrichment over the member-read `block_spec_references` RLS; the dedupe logic is unit-tested; surfaces are E2E-baseline + prod-verified. **Deferred (the rest of G6):** **G6.2** two-speed propagation (structured content auto-updates the working copy; prose flagged not rewritten — a Trigger.dev task), **G6.3** domain-ownership routing (4-step fallback) + **G6.4** review-item generation + **G6.5** the `dashboard_action_items` action queue with the accept/modify/dismiss modal (migration 0006 tables already exist), **G6.6** pre-commit "affects N documents" on a field save, **G6.7** published-snapshot isolation. The banner shows field names, not before→after values (the referenced `field_version`'s value is a join away — a follow-up). **Next: G6.6 pre-commit impact (small) or G6.3/G6.4/G6.5 the routing+dashboard spine.** |
| 2026-06-16 | G6.3 — domain-owner routing (M7, the review-routing keystone) | The resolver that decides *who* reviews a spec-change flag — the engine G6.2/G6.4 assign through. New **`@arther/types` `resolveDomainOwner`**: the pure four-step fallback per smart-spec-tracking spec §3.4 — product-specific override (`domain_ownership_config` for category+product) ▸ workspace category default (category, no product) ▸ document owner (`documents.owner_id`) ▸ workspace admin (`workspaces.owner_id`, final backstop); returns the owner **and** the `source` step (for "routes via …" affordances). Config is optional — a workspace with none routes to the document owner, so small teams work without modelling org structure. 7 unit tests (each level + strict precedence + unresolved). New **`@arther/db` `resolveDomainOwnersForDocument`**: per-category owner map for one document — reads `domain_ownership_config` (member-readable, pre-filtered to this product or workspace-default), the document product/owner, and the workspace owner, all under RLS, then applies the pure resolver. New **`tests/db` `domain-ownership.test.ts`** (6 probes): the 0006 matrix's singular-owner partial-unique indexes (one default per category; one override per category+product; the two scopes coexist) + Settings RLS (owner/admin write, member read, stranger blocked). No migration (0006 `domain_ownership_config` already shipped) and **no surface** — pure routing engine, like G3. Gates: turbo 39/39, db 137/137, drift in sync (18 files). | The Settings → Domain Ownership matrix UI (edit the config) and the per-product panel (§3.4) are the config-management follow-up; consumption (setting `section_review_items.assigned_to`) lands with G6.2 propagation + G6.4 review items. Until a config UI exists every category resolves to the document owner — correct per spec, and the probe exercises the override/default paths directly. |
| 2026-06-16 | G6.2 (first slice) — two-speed propagation engine (M7) | The cascade that makes spec changes flow into documents. New **`@arther/types` propagation engine** (pure, 14 unit tests): `classifyBlockSpeed` (the 20 block types split prose / structured / structural per spec §3.1), `rewriteSpecTokens` (recursively advances inline `spec_token` nodes — rich text, links, captions, safety + accordion/wizard children — to the new value snapshot), `attributeSections` (nearest preceding `section_header`), and `planFieldPropagation` (per-document plan: block content rewrites both speeds; prose sections flagged only when the doc is **published**). New **`@arther/db` `propagateFieldChange`** (service role + workspace scope): writes a `field_change_diffs` record, and per citing document auto-updates the working-copy tokens, **advances the stale `block_spec_references` anchors** (clears the G6.1 banner + keeps spec tables/charts live), and for published docs creates `section_review_items` routed via **G6.3** + a `dashboard_action_items` row and flips `document_review_states` to `needs_review`. **Wired inline** behind the field-save action (best-effort; the value is already committed) — moves to the Trigger.dev durable runner with G1.2. New **`tests/db/propagation.test.ts`** (5 probes): anchor-advance clears staleness; `field_change_diffs`/`document_review_states` are service-role-write-only; review item routes to its owner + dashboard item, members read; **published snapshot frozen (G6.7 / invariant 5)**. No migration (0006 tables already shipped). Gates: turbo 39/39, db 142/142, drift in sync (18 files). | Deferred: bulk `propagate-batch` (G6.2b), scalar-override / snippet / chart review-item types (G6.4), and the dashboard review modal (G6.5). Published-doc path is probe-exercised since publishing is Phase 3 (no published docs exist yet); in Phase 2 every edit keeps draft working copies current. Best-effort wiring logs (not surfaces) a propagation failure — staleness still reads correctly from the advanced version. |
| 2026-06-16 | G6.6 — pre-commit impact ("affects N documents") (M7) | Before a spec field's **global value** changes, the editor now shows the blast radius and asks the author to confirm. New **`@arther/types` `field-impact.ts`** (pure, ADR-012): `FieldChangeImpact` + `describeFieldChangeImpact`/`fieldChangeHasImpact`/`listImpactedDocuments` (independent document/block pluralisation, "+N more" overflow); 9 unit tests. New **`@arther/db` `getFieldChangeImpact`** — the distinct documents + blocks citing a field via `block_spec_references` (archived documents excluded), under the same member-read RLS G6.1 staleness reads through. **App**: `updateFieldValueAction` gains a two-phase confirm — opt-in via the editor's hidden `impactCheck`; an unconfirmed change *with* impact returns the blast radius (no write), the author confirms with `confirmed`, a zero-impact change commits with no friction; `FieldValueEditor` renders the confirm footer (impact line + "Apply change") on both the scalar-family and reference forms. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | Scoped to the **global** value editor (the clear cross-document case); **override** edits (product-scoped, narrower radius) and the **table editor** save directly (no `impactCheck` opt-in) — both follow-ups. No new DB probe: `getFieldChangeImpact` is an enrichment read over the `block_spec_references` member-read RLS the G3 `documents-blocks` probe already covers (the G6.1 precedent); the pure summary is unit-tested; the confirm needs provisioned data (no new E2E, per the data-bearing-editor precedent). Counts citations on the working copies that exist today (all drafts). Composes cleanly with the **G6.2 propagation engine** that landed in parallel (#58): the confirm previews the blast radius *before* the write, propagation cascades it *after*. The remaining **review-item types + action dashboard** (G6.4/G6.5) — which per spec §3.3 activate on *published* documents — are the next M7 slices, best sequenced with the Phase-3 publish/snapshot pipeline. |
| 2026-06-16 | G7.1 (first slice) — single-block regeneration (M7) | "Regenerate this block" against the current spec graph — the manual path today, and the resolution for a G6.1 staleness-flagged block. **`@arther/ai-gateway`**: `regenerateBlock` reuses the **one section contract** — the model returns a `GeneratedSection`, resolved through the **same zero-hallucination gate** as `generateSection` (G2.5), then the block of the target type is taken as the replacement; `buildSectionPrompt` gained a `focus` mode (rewrite ONLY this block; current text injected). 5 new unit tests (mock gateway: grounded replacement / zero-hallucination fail / gateway-throw / focus prompt). **`@arther/db` `block-regeneration.ts`**: `loadBlockRegenContext` (block → product + enclosing section via the reused G6.2 `attributeSections` + current text) and `applyBlockRegeneration` (replace content + **re-anchor `block_spec_references`** to the cited current versions). **App**: `regenerateBlockAction` authorizes **`doc.generate`**, builds the resolver + prompt from the product's current-valued fields + brief (like generation), runs `regenerateBlock` inline, writes via `applyBlockRegeneration`; degrades honestly without `ANTHROPIC_API_KEY`. **Editor**: a **Regenerate** button in the inspector for paragraph/callout blocks (labelled "Regenerate (spec changed)" on a flagged block), replacing the content in place on success. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | First G7.1 slice — **manual single prose-block regen** (paragraph/callout, the prose family staleness flags). Deferred: other block types, **spec-change-triggered auto-regen** (the button is the manual bridge), multi-block/section regen, and run-tracked idempotency (the `block_regeneration` GenerationRunKind exists; G8.1). No new DB probe — `applyBlockRegeneration`/`loadBlockRegenContext` are editor-gated writes/reads over the `blocks`/`block_spec_references` RLS the G3 `documents-blocks` probe already covers (the G4.6 precedent); the deterministic core is unit-tested and section attribution reuses G6.2's tested `attributeSections`. `applyBlockRegeneration` is a 3-step PostgREST sequence (not atomic) — acceptable for an authoring action (createDocument precedent); the atomic RPC is a follow-up. Regen offers all the product's current-valued fields as citable (not section-category-filtered) and omits brand voice — thin follow-ups. **Next M7: G7.2 placeholder fill / G7.3 brief-referenced staleness; or the publish-gated G6.4/G6.5 dashboard spine with Phase 3.** |
| 2026-06-16 | G4 — live spec_table rendering (M6) | Generated documents now render **spec_table** blocks as a real table of **current spec values**, not a placeholder (spec §3.1: read live at render time through the one shared renderer). New **`@arther/types` `spec-table.ts`** (pure, ADR-012): `ResolvedSpecField`/`SpecFieldResolution` (the `field_id`→current-value map the renderer is handed) + `deriveSpecTableCells` — scalar→typical, range→min/max, toleranced→nominal ± bounds, other types→one formatted value, **null→em dashes** (invariant 6, never a fabricated number); 7 unit tests. New **`@arther/db` `resolveSpecFields`** (reuses `loadGenerationFields` + the unit registry → the resolution map, RLS). **`@arther/block-renderer`** `BlockRenderer` gains an optional **`resolved`** prop threaded through the tree; **spec_table** renders an HTML table (Specification + configurable Min/Typical/Max/Conditions/Source per `column_config`, visible rows in `display_order`, `decimal_places` honoured) — absent resolution degrades to the prior placeholder (so the portal/PDF wire it in unchanged at C6/C5). **App**: the read-only document view and the editor (preview + canvas) resolve the product's field values server-side (only when a spec_table is present) and pass `resolved` down. 1 new renderer test (live table: scalar value / range min+max / source / hidden-row omitted); the no-resolution placeholder test still holds. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | **spec_table only** this slice; **chart** (needs the table-field `TableValue` → the existing `SpecChart` SVG primitive) reuses the same `resolved` prop and is the immediate next slice, along with the remaining media placeholders (video/gif/hotspot/snippet/toc). No new DB probe — `resolveSpecFields` is an enrichment read over the spec-graph RLS the G3/spec probes already cover (the G6.1 precedent); the pure derivation + the renderer are unit-tested via `renderToStaticMarkup`. Conditions render as `—` (the scalar family carries none; table-field conditions arrive with chart). The renderer carries class names but the package ships no CSS yet (consistent with the existing prose blocks) — table styling is a polish follow-up. **Next: chart live rendering (same prop); then the brief-reference spine (unblocks G7.2/G7.3) or the Phase-3 publish track.** |
| 2026-06-16 | G4 — live chart rendering (M6) | Generated documents now render **chart** blocks as a real SVG plot of the referenced table field's live value, not a placeholder — the immediate follow-on to spec_table (#61), reusing the same `resolved` prop. A chart's `table_field_id` resolves through the existing `resolveSpecFields` map (a table field's value *is* a `TableValue`), so **no DB change**. **`@arther/block-renderer`** `Chart` maps the resolved `TableValue` (columns/rows/interpolation) onto the existing **`SpecChart`** SVG primitive (`@arther/ui`) — the architecture's "the Chart block renders through this same component so editor preview and published output match" (F6.3 note). New dep `@arther/block-renderer → @arther/ui` (SpecChart is pure SVG, no `'use client'`, no side-effect imports — safe for SSR/PDF; tree-shaken to just the chart path). Absent/empty/mis-typed resolution degrades to the prior placeholder. Both document pages now resolve when a spec_table **or chart** is present. 2 new renderer tests (live SVG plot with the dependent-column label + block title; placeholder without resolution). Gates `pnpm turbo lint typecheck test build` **39/39**, block-renderer **16/16**, E2E **50/50**, drift in sync (no migration). | `SpecChart` is a **line** primitive: the chart block's `chart_type` (scatter/bar), `x/y_axis_label` overrides, and `show_legend`/`show_grid` toggles aren't honoured yet — they extend `SpecChart` in a follow-up (it derives axis labels from the mapped columns + always draws line+points). No new DB probe — chart resolution rides the `resolveSpecFields` read the spec-graph RLS already covers (G6.1/#61 precedent); rendering is `renderToStaticMarkup`-tested. When the **portal** wires block-renderer at C6 it must transpile `@arther/ui` too (only SpecChart is reachable, so the bundle stays lean). **Remaining renderer placeholders: toc + media (video/gif/hotspot/snippet). Next: those, or the brief-reference spine (unblocks G7.2/G7.3), or the Phase-3 publish track.** |
| 2026-06-16 | G4 — toc + media block rendering (M6) | The last renderable placeholders in the shared renderer now render fully. **toc** builds a real table of contents from the document's section headers + headings (reusing `blockPlainText`), filtered by the block's `depth`, with anchor links — each top-level section_header/heading now carries a `br-block-{i}` id the toc links to. **video** → `<video controls poster>` (a download link as fallback), **gif** → `<img>`, **hotspot_image** → the base image with **percent-positioned numbered pins** + a numbered legend — all with their optional rich-text captions. `snippet` stays a labelled placeholder (Content Reuse is Phase 4; its content isn't resolvable yet). 7 new renderer tests (toc links + depth filter, video/gif/hotspot, snippet placeholder); 22 in `@arther/block-renderer`. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | Pure render additions — no DB, no app surface (the read-only view + editor already pass `blocks`; toc/media render from block content alone), so no new E2E. **video** uses `controls` and ignores `autoplay` in the read view (intrusive; the editor can opt in later); **hotspot** pins position over the natural-size image (responsive scaling + pin→spec-field linking are post-MVP per the schema). The renderer still ships no CSS (semantic classes only, consistent with the prose blocks) — toc indent + hotspot pin styling are a polish follow-up. **Every block type now renders except `snippet` (Phase 4). Next: `SpecChart` config (chart_type / axis labels / legend), the brief-reference spine (unblocks G7.2/G7.3), or the Phase-3 publish track — the renderer is now portal/PDF-ready.** |
| 2026-06-16 | G4 — SpecChart config (chart_type / axis labels / legend / grid) (M6) | The Chart block's configuration now drives the rendered chart, closing the #62 follow-up. **`@arther/ui` `SpecChart`** gains optional props (defaults preserve the line-chart behaviour, so the F6.3 table-editor preview is unchanged): **`chartType`** — `line` (path + points), `scatter` (points only), `bar` (rects from the y=0 baseline); **`xAxisLabel`/`yAxisLabel`** drawn as SVG axis titles (default to the mapped column names); **`showLegend`** — a series legend (colour swatch + value) when a series column exists; **`showGrid`** — dashed min/mid/max gridlines behind the data. Asymmetric padding makes room for the axis titles. **`@arther/block-renderer`** `Chart` passes the chart block's `chart_type`/`x_axis_label`/`y_axis_label`/`show_legend`/`show_grid` straight through. New `ui-chart__axis-label`/`__grid`/`__legend`/`__swatch` styles in `components.css`. 3 new renderer integration tests (bar→rects-not-path, grid + axis-label override, legend with a series column); 25 in `@arther/block-renderer`. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | SpecChart's render is verified through its block-renderer consumer (which has the `renderToStaticMarkup` harness) — `@arther/ui` ships no `react-dom` in its test env (pure-function tests only), so no unit test was added there. `scatter`/`bar` keep the ≥2-point guard (a 1-point chart still shows the empty hint — a minor edge). Bars use a fixed width and overlap when multiple series share an x (acceptable for the preview primitive). **The renderer's chart support is now feature-complete. Next: the brief-reference spine (unblocks G7.2/G7.3), editor polish (drag-reorder / block-type insert / find-replace), or the Phase-3 publish track.** |
| 2026-06-16 | G4.6 — insert non-paragraph block types (M6 editor) | The editor's only insert was "+ Paragraph"; you can now add a **heading, callout, section header, or divider** to a draft. New **`@arther/types`** (block-content, ADR-012 one source): `INSERTABLE_BLOCK_TYPES` + `insertableBlockTypeSchema` + `defaultBlockContent(type)` (a valid empty `BlockContent` per type — prose with empty rich text, callout `info`, H2, empty-titled section header, bare divider) + `insertableBlockLabel`; a unit test round-trips every default through `blockContentSchema`. **App**: `addBlockAfterAction` generalised from hardcoded-paragraph to a `type` param (defaulted, validated with classic-v3 `z.enum(INSERTABLE_BLOCK_TYPES)` — the v4 `insertableBlockTypeSchema` can't embed in a v3 `z.object`), building the block via `defaultBlockContent`. **Editor**: the toolbar's "+ Paragraph" becomes a **block-type `<select>` + "+ Insert"**; the new block is inserted after the selection and selected (prose edits inline via TipTap, section-header title + callout/heading attrs via the inspector G4.2, divider renders as-is). Gates `pnpm turbo lint typecheck test build` **39/39**, `@arther/types` **173**, E2E **50/50**, drift in sync (no migration). | Scope held to the **prose + structural** family that already has inline editing or inspector property editors (G4.2) and renders fully — data/media/container types (spec_table/chart/image/accordion/…) need a field/source/config picker and are inserted through their own flows (follow-ups). `code_block` excluded (no content editor yet). Insertion stays a structural op (blocked offline, G5.5). No new DB probe — `addBlockAfterAction` is the G3-probe-covered `insertBlocks`/`reorderBlocks` path with a different content default; the defaults are schema-validated in the types unit test. **Next editor polish: drag-reorder / multi-select / find-replace (G4.7); or the brief-reference spine, or the Phase-3 publish track.** |
| 2026-06-16 | G4.7 — find & replace (M6 editor) | The block editor gains **find & replace** over a document's text. New **`@arther/types` `text-search.ts`** (pure, ADR-012): `countMatchesInBlock` + `replaceInBlock` (and the shared `mapBlockText` traversal) — literal, case-sensitive matching across a block's editable text: rich-text `text` nodes, the text inside links, callout titles, and section-header titles; **inline spec tokens are resolved values and are never matched or rewritten** (invariant 6); marks are preserved; a no-match replace returns the original content unchanged. 7 unit tests. **Editor**: a **Find** toolbar toggle opens a find/replace bar — query with a live match count, **Prev/Next** that scroll-select matching blocks (highlighted with an accent wash, distinct from the staleness bar), a replacement field, and **Replace all** (reports "Replaced N in M blocks"). Replace persists each changed block through the G5 save queue (works offline-queued) and **bumps a `replaceVersion` key so the inline TipTap editors remount** with the new text. Gates `pnpm turbo lint typecheck test build` **39/39**, `@arther/types` **180**, E2E **50/50**, drift in sync (no migration). | Scope: **case-sensitive, literal, replace-all** over top-level blocks. Deferred: case-insensitive/regex toggle, **replace-one-at-a-time** (Next/Prev navigate; Replace is all-at-once), and matches **inside container children** (accordion/wizard/safety) — `mapBlockText` rewrites the top-level prose + titles, not nested content (a clean extension of the same traversal). `code_block` text is intentionally not searched (avoids surprising edits in code). No new DB probe — replace writes through the G3-probe-covered block-content save path; the pure match/replace logic is unit-tested. **Next editor polish: drag-reorder / multi-select, or nested-content + replace-one; or the brief-reference spine, or the Phase-3 publish track.** |
| 2026-06-16 | G8.5 — cost guardrails (prompt caching + generation rate cap) (M8 harden) | Two cost/abuse guardrails on the paid AI path. **Prompt caching:** **`@arther/ai-gateway`** `structured` gains an opt-in `cacheSystem` — the system prompt is sent as a `cache_control: ephemeral` text block instead of a plain string (Anthropic prompt caching), with the `MessagesClient` system type widened to `string | TextBlock[]`. The generator's `buildSectionPrompt` now keeps its **rules byte-stable across a run's sections** (the section name moved from the system rules into the user message), and `generateDocument` sets `cacheSystem` so sections 2..N (and re-runs within the cache window) reuse the cached prefix; a one-off block regeneration leaves it off (no cache-write premium). **Rate cap:** new **`@arther/rate-limit`** `generation` budget (10/60s per member) wired into **both** `createGenerationRunAction` and `regenerateBlockAction` (after canDo/auth, before the model call), keyed by member — the import-throttle pattern. 3 new unit tests (gateway: cached block only when opted in; generation: identical system across two section names + section in the user msg; rate-limit: generation budget enforced + per-member). Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | Caching is opt-in per request so import (one-off) doesn't pay the cache-write premium; only multi-section generation (and same-doc-type re-runs within ~5 min) benefit. The rate cap shares one budget across generate + regenerate (both paid AI calls). No new DB probe (no SQL); gateway/limiter behavior is unit-tested via the `client` test seam + the deterministic limiter clock. **Remaining G8 (all DB-probe-shaped — need CI's Postgres, no docker locally): G8.1 idempotency (largely covered by the 0018 double-commit guard + the regen replace path), G8.2 analytics events, G8.3 extended RLS probe, G8.4 100+-block volume test. Next: those, the brief-reference spine, or the Phase-3 publish track.** |
| 2026-06-16 | G8.2 — analytics events (M8 harden) | The app now emits the three workspace metering events into the 0011 `analytics_events` store (no migration — the table + member-read RLS + append-only triggers already shipped). New **`@arther/db` `recordAnalyticsEvent`** — a **service-role** append (the table has no authenticated INSERT policy; events come from trusted server paths only), workspace-scoped via `scopedServiceQuery`. Wired **best-effort** (a metering write never fails the user action) into **`document_generated`** (the inline generate run, with the committed document id + run/product/type/token payload), **`block_regenerated`** (G7.1 regen, with block id/type), and **`spec_field_updated`** (the spec value save, beside the G6.2 propagation). New `tests/db/analytics.test.ts` (3 probes: service-role write + member read / stranger isolation, authenticated-insert denied, append-only update+delete blocked). Gates `pnpm turbo lint typecheck test build` **39/39**, db-tests typecheck clean, E2E **50/50**, drift in sync (no migration). | Metrics are SQL aggregates over the store (the §A admin dashboards are Phase 4); this is just the emit hook. Portal events (document_viewed/…) emit from the portal at the C-track. The emit uses a fresh `createServiceClient()` per call (cheap; matches the propagation pattern). **Remaining G8: G8.3 extended RLS probe + G8.4 100+-block volume test (next, as a probes PR); G8.1 idempotency confirmed already covered — the 0018 double-commit guard probe + `single-handler` + the regen replace path.** |
| 2026-06-16 | G8.1/G8.3/G8.4 — G8 hardening complete (M8) | Closes the Phase-2 hardening epic. **G8.3 (extended RLS probe):** new `tests/db/rls-probe-phase2.test.ts` extends the F8.1 second-user probe to the Phase-2 spine — user B in W2 can't read W1's documents / document_revisions / blocks / block_spec_references / dashboard_action_items / domain_ownership_config, nor mutate W1 blocks or insert into W1 (with-check). Seeded with the proven document graph (built-in Document Type, real revision/block/ref + an action item + a domain owner). **G8.4 (realistic-volume test):** new `tests/db/volume.test.ts` builds a **120-block** document with a spec reference on every block; asserts the full ordered block-tree load + the indexed staleness join (`field_version_id <> current_version_id`) stay correct and complete — 0 stale, then **one field bump flips all 120 stale** (the staleness index covers it at volume). **G8.1 (idempotency):** confirmed already covered — the 0018 `commit_generation` double-commit guard (`generation-commit.test.ts`), the F8.6 `single-handler` trigger probes, and the regen replace-not-append path (`applyBlockRegeneration` clears refs before re-inserting); no new code. Gates `pnpm turbo lint typecheck test build` **39/39**, db-tests typecheck clean, E2E **50/50**, drift in sync (no migration; tests/db-only). | DB probes authored against the migration set, run in **CI's Postgres** (no docker this session) — the analytics probe (#68) just confirmed the seeding/assertion patterns. G8.3 consolidates the cross-tenant gate (each Phase-2 table already had its own per-feature isolation test). The volume test asserts correctness + completion rather than a wall-clock bound (timing assertions are flaky). **G8 — Phase-2 hardening — is COMPLETE** (G8.1 confirmed · G8.2 #68 · G8.3/G8.4 here · G8.5 #67). **Next: the brief-reference spine (unblocks G7.2/G7.3), or the Phase-3 publish track (C4–C6) — the v1 critical path.** |
| 2026-06-16 | Brief-reference spine — generation records `block_brief_references` (unblocks G7.3) | The brief analog of `block_spec_references`: generation now anchors each brief-sourced block to the fragment it drew from, so a later brief edit becomes detectable. **No migration, no commit-RPC change** — written app-side after the atomic commit. **`@arther/types`**: the generated-block contract gains an optional **`brief_key`** (on every wrapper; only brief-sourced prose sets it), and `resolveGeneratedSection` carries it into `GenerationCommitBlock.briefKey` (null unless `source==='brief'`). **`@arther/ai-gateway`** `buildSectionPrompt`: a new stable rule tells the model to set `source:'brief'` + `brief_key` (the `###` fragment headings) when a block's narrative comes from the brief. **App** (`runGenerationInline`): after `commitGeneration`, loads the committed blocks (same array order → index↔block-id) and writes a `block_brief_reference` (via the existing `addBriefReference`, **service role**) for each brief-keyed block whose key matches a real product-brief fragment, capturing the fragment's **content snapshot**; best-effort (never fails the generation). **`@arther/db` `listStaleBriefReferencesForDocument`** — the G7.3 read foundation: refs whose `content_snapshot` ≠ the fragment's current content (compared in JS; PostgREST can't join on `(brief_id, fragment_key)`). New `tests/db/brief-staleness.test.ts` (write + member-read/stranger isolation + snapshot-matches→not-stale, edit→stale) and an assembler unit test (briefKey carried for brief blocks, null otherwise). Gates `pnpm turbo lint typecheck test build` **39/39**, `@arther/types` **181**, db-tests typecheck clean, E2E **50/50**, drift in sync (no migration). | Scoped to the **product** brief (what generation injects today); component-brief references follow with the graph-brief assembly. The index→block-id map relies on `commit_generation` inserting blocks in array order (it does, G2.6); a tiny TOCTOU between commit and the brief-ref write is acceptable (best-effort, advisory). `block_brief_references` RLS + one-per-block uniqueness were already G3-probe-covered; this probe adds the **staleness compare**. **G7.3** (the light "brief updated" banner/indicator) is now a thin surface over `listStaleBriefReferencesForDocument` — the mirror of G6.1. **G7.2** (placeholder fill) still needs the separate `placeholder_brief_references` spine (G2.7). |
| 2026-06-16 | G7.3 — brief-referenced staleness surface (M7) | The thin surface over the brief-reference spine, mirroring G6.1 for briefs: editing a brief fragment now **lightly flags the blocks generated from it**, distinct from spec urgency. New **`@arther/types` `summarizeBriefStaleness`** (pure: dedupe edited fragment keys + affected blocks; 2 unit tests). Surfaced via the existing **`listStaleBriefReferencesForDocument`** (the spine PR's read) on the **read-only document view** and the **editor**: a **light** "N brief fragment(s) updated since generation (keys) — the prose may want a refresh" note (muted `specs-grid__meta`, not the spec banner's error styling), and the editor **marks affected blocks with a light info bar** (`inset 2px info`) — visually distinct from spec staleness's solid warn bar (a block that's both shows the urgent warn bar). Generate → edit a cited brief fragment → the document/editor flags exactly which prose drew on it. Gates `pnpm turbo lint typecheck test build` **39/39**, `@arther/types` **183**, E2E **50/50**, drift in sync (no migration). | No new DB probe — `listStaleBriefReferencesForDocument` is the spine PR's read (already probe-covered by `brief-staleness`); the summary is unit-tested; the surfaces are UI over an existing read (E2E baseline + prod-verified, like G6.1). The banner lists fragment keys, not before→after content (a follow-up). **M7 brief-tracking loop closed: brief edit → flagged prose.** **Remaining M7: G7.2 placeholder fill (needs the separate G2.7 `placeholder_brief_references` spine). Next: G2.7+G7.2, or the Phase-3 publish track (C4–C6 — the v1 critical path).** |
| 2026-06-16 | G2.7 — placeholder blocks for required-but-empty brief fragments (M7) | Generation no longer silently skips a section whose **required** brief fragment is empty — it emits a visible **placeholder block** (a callout "This section needs the '{key}' brief fragment", `source='placeholder'`, can't be published) + a `placeholder_brief_references` row, so filling the fragment can later find the blocks waiting on it (G7.2). Mirrors the brief-reference spine — **no migration** (0005 tables exist). **`@arther/types`**: `GenerationCommitBlock` gains `placeholderBriefKey`. **`@arther/ai-gateway`**: `SectionPlan.missingBriefKeys`; `generateDocument` appends a placeholder callout per missing key after each section (a header is emitted even when the model produced nothing, so an all-placeholder section still renders). **App** (`runGenerationInline`): computes each section's required-but-empty keys (`brief_required` ∧ key not a populated fragment) into the plan, and after commit writes `placeholder_brief_references` (entity=product, fragment_key, section_name from the preceding header) — folded into the brief-ref loop. 2 ai-gateway unit tests (placeholder appended on success + on a failed section) + `tests/db/placeholder-refs.test.ts` (the `(entity, fragment_key)` lookup + one-per-block uniqueness + member-read/stranger isolation). Gates `pnpm turbo lint typecheck test build` **39/39**, `@arther/ai-gateway` **20**, db-tests typecheck clean, E2E **50/50**, drift in sync (no migration). | Scoped to **product**-brief required fragments (what generation injects). The placeholder is a `callout` (no dedicated placeholder block type — `source='placeholder'` carries the semantics; a distinct editor/renderer treatment + the publish-time can't-publish gate are follow-ups with Phase 3). **G7.2** (filling a fragment offers to regenerate its waiting placeholders) is the next slice — a thin layer over the new `placeholder_brief_references` + G7.1 regeneration. |
| 2026-06-16 | G7.2 — placeholder fill offer (M7 complete) | Filling a brief fragment now **offers to auto-generate the blocks waiting on it** (the G2.7 placeholders). New **`@arther/db`**: `listPlaceholdersForFragment(entity, key)` (the waiting placeholder blocks) + `clearPlaceholder(blockId)` (drop the `placeholder_brief_reference` + retag `blocks.source` placeholder→brief once filled). **App**: `saveBriefFragmentAction` now returns the **waiting count** after a content save; new `fillPlaceholdersAction` regenerates each waiting placeholder via the **G7.1 `regenerateBlockAction`** (the block now has brief content to draw on; doc.generate-gated + rate-limited per block inside it) and clears its marker. **`BriefForms`**: after saving, an offer — "N blocks are waiting on this fragment → Fill N blocks" — then "Filled N." Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | Reuses the G7.1 regen pipeline by importing `regenerateBlockAction` across routes (server-action → server-action; no cycle). The placeholder is a callout, so fill regenerates a callout in place (real content, source flipped to 'brief') rather than swapping the block type — a type-swap to section prose is a follow-up. No new DB probe: the lookup is exactly the G2.7 `placeholder-refs` probe's query; `clearPlaceholder` is editor-write over the G3-probe-covered `blocks`/`placeholder_brief_references` RLS (the G4.6 precedent). **M7 — Smart Spec Tracking — is COMPLETE** (G6.1/6.2/6.3/6.6, G7.1/7.2/7.3 + the spec & brief reference spines + the G2.7 placeholder spine). **Next: the Phase-3 publish track (C0–C6) — the v1 critical path.** |
| 2026-06-16 | G4.2 — block property editors (data/media/leaf types) (M6) | Extended the inspector beyond the prose family to the configurable **data/media/leaf** block types — selecting one now edits its structural attributes (committed through the same `blockContentSchema`-validated save path). **`toc`** (title + depth), **`code_block`** (language/caption/code textarea), **`image`** (url/alt/width), **`video`** (url/thumbnail/autoplay), **`gif`** (url/alt), **`hotspot_image`** (url/alt + pin count), **`spec_table`** (title + the full `column_config` — show min/typical/max/conditions/source toggles, unit preference, decimal places), **`chart`** (title + chart_type + axis-label overrides + legend/grid toggles). New reusable inspector controls (`BoolProp`/`TextAreaProp`/`NumberProp`) alongside TextProp/SelectProp; `BlockProperties` refactored to a switch. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | UI-only — `{...content, attr}` spreads re-validated by `blockContentSchema`; no new unit test (typecheck/build + the editor E2E baseline, per the G4.2 first-slice precedent). **Deferred:** container section/step management (accordion/step_wizard/safety children — nested editing) + the field/source pickers (spec_table rows, chart table field) are the remaining G4.2 slices; media editors are forward-looking until G4.6's block-type picker expands beyond prose/structural. **Buildable-now batch: G4.2 done.** |
| 2026-06-16 | G4.6 (first slice) — drag-to-reorder blocks (M6) | The editor canvas blocks are now **drag-reorderable**: each block grows a grip handle (`⠿`); dragging it and dropping on another block moves it **before** that block, persisting via the existing `reorderBlocksAction` (+ optimistic local state, an accent drop-line on the hover target). Works for prose and non-prose blocks alike — the handle is the only draggable element, so TipTap text selection is untouched; blocked offline (G5.5), and the Move up/down buttons stay as the fine-control fallback. Gates `pnpm turbo lint typecheck test build` **39/39**, E2E **50/50**, drift in sync (no migration). | UI-only client interaction (HTML5 DnD, no library); the reorder write is the G3-probe-covered `reorderBlocks` path. No new unit/E2E (drag is pointer interaction — covered by typecheck/build + the editor E2E baseline + prod-verify). Drop semantics are "before the target" (the Move buttons cover end-placement). **Remaining G4.6: multi-select + bulk ops, copy/paste across docs (next slices). Buildable-now batch: G4.2 ✓, G4.6 drag ✓.** |
| 2026-06-16 | G4.6 — multi-select + bulk delete (M6) | The editor canvas now supports **multi-block selection**: a plain click selects one block; **⌘/Ctrl-click** toggles a block in/out of the selection; **Shift-click** extends a contiguous range from the anchor. Selected blocks all carry the accent outline (and outline-nav `aria-current`), and when more than one is selected the Properties inspector swaps the single-block editor for a **bulk panel** ("N blocks selected" + **Delete selected** / Clear). Bulk delete fans `deleteBlockAction` across the set (`Promise.all`), prunes the locally-removed blocks, and clears the selection; structural so it stays offline-blocked like the other ops. Single-block flows (inspector, move up/down, regenerate, insert-after, find/replace navigation) are unchanged — they read a derived `selected = selectedIds.size === 1 ? the-one-id : null`. The modifier-click set algebra (`toggleSelection`, `rangeSelection`) is extracted to **`@arther/types`** `block-selection.ts` as pure helpers with 8 new unit tests (191 types tests). | Selection interactions stay unit-tested (the editor E2E has no provisioned doc data, per its own note); bulk **move**/copy-paste are follow-up G4.6 slices |
| 2026-06-16 | G4.6 — copy/paste blocks across documents (M6) | Builds on multi-select: **Copy** (single-block inspector + bulk panel) writes the selected blocks’ content, in document order, to a localStorage-backed clipboard so it **survives navigation between documents**; a toolbar **Paste (N)** button inserts them after the current selection (or at the end) as fresh **`manual`** blocks. New server action `pasteBlocksAction` re-validates every block through `blockContentSchema`, bulk-`insertBlocks` (re-projecting `text_content` for FTS), and reorders the revision so the pasted run lands in place — returning the new ids so the client splices + selects them optimistically. Copy carries the **content only**, never the source doc’s spec/brief references (pasted prose is hand-placed, not generated). The clipboard read/write lives in a small client `clipboard.ts` (validated on read, capped at 100). Copy works offline; paste is structural so it stays offline-blocked. | Clipboard payload validation reuses `blockContentSchema` standalone (zod/v4) and the action keeps the scalar envelope on classic zod (v3) — the two majors are never mixed in one schema (the #65 build trap). Editor surface stays unit/CI-covered; E2E has no provisioned doc to drive the buttons |
| 2026-06-16 | G6.8 — spec coverage report (M6, Smart Spec Tracking §3.7/§5.7) | A new **Coverage** tab on the product spec view answers "which spec fields are documented?": a product-level summary (**N of M spec fields referenced in at least one document**) over per-group rows — product fields + each attached component — where every field is **✓ Referenced in N documents** or **○ Not referenced in any document** (the coverage gaps). Computed at read time from `block_spec_references` scoped to the product’s live (non-archived) documents; counts distinct documents per field. New DB helper `getSpecCoverageForProduct` (two RLS reads + JS aggregate — PostgREST can’t `count(distinct)` across a `documents.product_id` filter); the pure aggregation (`aggregateSpecCoverage`/`summariseCoverage`) lives in **`@arther/types`** with 9 unit tests (199 total), and a CI DB probe asserts distinct-document counting, archived-doc exclusion, and workspace-private RLS. | Counts all live documents (not strictly *published*) — the publish flow is gated this phase, so a published-only filter would read empty; it narrows once publishing lands. The "click an uncovered field → which doc types would include it" hint (§5.7) and variant-awareness (G6.9) are follow-ups. Surface needs provisioned data, so it stays unit/probe-covered (no E2E driver) |
| 2026-06-16 | G2.8 — live generation status surface (M6, AI Generator §5.1) | The run-outcome surface (after Confirm, `?run=`) is now a **client component** that goes live: while a run is **running** it polls `getRunStatusAction` (a lean, RLS-read projection of run + sections in order) every 2s so section statuses advance without a manual refresh — the documented **poll fallback** (a Supabase Realtime push can later kick the same refetch for instant updates). When a run the user is *watching* finishes successfully with a committed document, it **opens straight into the editor** (`/documents/{id}/edit`) — "opens into the editor on completion"; revisiting an already-finished run just shows the result with an **Open in editor** / **View document** pair. The poll/open decisions are pure helpers in **`@arther/types`** (`shouldPollRun` — only `running` is live under the inline model; `shouldOpenEditorOnCompletion` — watched + succeeded/partial + has document) with 7 new unit tests (206 total). Reuses the G1.4 read model (`summarizeRunProgress`, `isTerminalRunStatus`). No migration. | Polling activates fully once generation is **async** (G1.2 Trigger.dev) — under the current **inline** model the action blocks until terminal, so the page loads already-final and the poll/auto-open is forward-compatible (and never spins: `queued`/terminal aren’t polled). **True Supabase Realtime** is the next layer — needs the two generation tables added to the `supabase_realtime` publication (a migration) + a browser Supabase client; the refetch action is the seam. Surface needs a provisioned run, so it stays unit-covered (generate E2E baseline unchanged, 50/50) |
| 2026-06-16 | G6.2b — batch propagation (import → digest) (M6, Smart Spec Tracking §5.2) | Import commits now emit **one** propagation pass for the whole session instead of leaving citing documents silently stale. New DB helper `propagateImportBatch` (service-role, `scopedServiceQuery`): a **single staleness sweep** over every `set_value` field the import changed, grouped per document → field; the structured auto-update (inline-token rewrite + `block_spec_references` anchor advance) is identical to the single-field G6.2 path, but the prose-review fan-out is **coalesced per (document, section, assignee)** — a re-import moving a dozen values that touch one section becomes **one** `section_review_items` row listing a dozen `field_change_diffs`, not a dozen rows — and each assignee gets **one digest** `dashboard_action_items` per document ("Review N sections — spec values changed on import") rather than one per field. The coalescing is a pure **`@arther/types`** helper (`coalesceReviewSections` — union of blocks, de-duped diffs, shared-category-or-null) with 5 unit tests (211 total). Wired into `commitImportAction` best-effort after the 0015 commit RPC (values are already applied; a propagation hiccup never fails the commit), passing only the `set_value` field ids (newly created fields are cited by nothing yet). No migration. | Coalescing is unit-tested; the SQL surface it writes (anchor advance, `section_review_items`/`dashboard_action_items`/`document_review_states` + RLS) is already proven by the G6.2 probe, and probes (raw SQL) can’t call the JS helper, so no new probe. **Cross-document, per-assignee digest *delivery*** (email/in-app) is the Feature 6 notification system’s job (deferred) — this coalesces the dashboard items it reads. Runs inline behind the commit; moves to the Trigger.dev durable runner with G1.2. Import surface needs a provisioned commit, so it stays unit-covered (import E2E baseline unchanged) |
| 2026-06-16 | G5.2 — durable save queue (localStorage) (M6) | The editor’s auto-save queue now **survives a reload or crash**: with a per-document `persistKey`, `useSaveQueue` mirrors the pending edits to localStorage on every change and **rehydrates** them before first render. On mount the queue re-drains to the server, and `restored` lets the editor apply the unsent edits over the server’s blocks (remounting the inline editors via the `replaceVersion` bump) so prose shows them immediately — so killing the network mid-edit *and* reloading the tab loses nothing, not just staying offline. The snapshot/restore is pure **`@arther/types`** `SaveQueue.entries()` / `hydrate()` (entries include inflight items — a save unconfirmed before reload is retried, not dropped) with 4 new unit tests (215 total). Corrupt/oversized storage is swallowed (the in-memory queue still drives saving) and `updateBlockContentAction` re-validates every value through `blockContentSchema`, so a stale or tampered entry can never corrupt a block. No migration. | Closes the G5.2 durability gap (queue was memory-only). **Advisory optimistic lock + "who’s editing" (G5.1) and the reconnect block-level keep-mine/use-server conflict (G5.4)** are the next G5 slice — they need per-block server-version threading (the `blocks.last_edited_at` token) through the save path + a conflict UI; presence needs Realtime (the G2.8 seam). Editor surface needs provisioned data, so it stays unit-covered (50/50 E2E unchanged) |
| 2026-06-16 | G5.1/G5.4 — optimistic lock + block-level reconnect conflict (M6) | Block content saves are now **optimistically locked** on `blocks.last_edited_at`: the editor seeds each block’s version token from the server load and threads it through every save; new DB helper `saveBlockContent` does a **conditional UPDATE** (write only if the token still matches) and, on a mismatch, reads back the server’s current block and returns a **conflict**. The editor surfaces a block-level **Edit conflict** banner — per block, **Keep mine** (adopts the server token, then re-saves your content so it lands) or **Use theirs** (replaces local content with the server’s and remounts the inline editor). Untracked blocks (just inserted / pasted / regenerated) save unconditionally and re-establish their token (regenerate drops its stale token so there’s no false self-conflict). `block_spec_references`-style version threading runs through the existing offline save queue, so a same-block conflict surfaces both live and on reconnect-drain. `BLOCK_COLUMNS` now carries `last_edited_at`/`last_edited_by`; `updateBlockContentAction` takes the expected token and returns `{ ok, conflict?, lastEditedAt? }`. New CI DB probe asserts the conditional-update semantics (matching token writes one row + advances it; a stale token writes none). No migration. | Delivers the G5.4 acceptance ("a same-block conflict surfaces a block-level choice"). The **G5.1 "who’s editing" presence banner** is the remaining piece — it needs Realtime presence (the deferred `supabase.channel` seam from G2.8), so it’s noted as a follow-up. Multi-user conflict isn’t E2E-drivable here, so it’s covered by the conditional-update DB probe + the action/queue wiring (50/50 E2E unchanged) |
| 2026-06-16 | G4.7 — workspace search (docs · spec values · library) (M6) | The TopBar’s ⌘K search now goes somewhere: a new **`/search`** surface searches three of G4.7’s four scopes (the fourth, in-doc find/replace, is the editor’s). New DB helper `searchWorkspace` (user client, RLS-scoped): **documents** via full-text over block prose (`blocks.text_search @@ websearch_to_tsquery`), narrowed to each document’s **current revision** and non-archived docs, one hit per doc with a snippet; **spec values** via `spec_fields.name ilike` (wildcards escaped); **library** via `components.name ilike` — each capped, rendered grouped with deep links (doc → viewer, field → its detail / the library, component → the library). Server-rendered **GET form** so a query is shareable/bookmarkable and works without JS. The snippet builder is a pure **`@arther/types`** `searchSnippet` (centres a window on the matched term with ellipses) with 5 unit tests (220 total). TopBar gained an optional `searchHref` (the app points it at `/search`; the portal keeps the bare button) — the control is a link now, so two shell a11y specs updated (link vs button) + a new `search.spec.ts` baseline (52 E2E). No migration. | In-doc find/replace already shipped (G4.7 earlier). Document search matches **prose** (`text_content`), not resolved spec tokens (invariant 6) — noted in the empty state. A full **⌘K command-palette** overlay (vs a routed page) and value-text search are follow-ups. searchWorkspace’s RLS/FTS is exercised by the surface; a provisioned-data probe can follow |
| 2026-06-16 | F4.5 — workspace logo upload (Storage) (M6) | Owner/admins can now set a **workspace logo** from `/settings`: a file upload to a public **`workspace-logos`** Storage bucket whose public URL pins onto `workspaces.logo_url` (the column already existed). New action `uploadWorkspaceLogoAction` (owner/admin-gated via `workspace.manage`) type-checks (PNG/JPEG/SVG/WebP/GIF) and size-caps (2 MB) the image, uploads at `{workspaceId}/logo-{ts}.{ext}` (`upsert`), then `getPublicUrl` → `updateWorkspaceLogo`; a **missing bucket degrades to an honest message** (no crash), mirroring the F7 import-upload pattern. `removeWorkspaceLogoAction` clears it. `getActiveWorkspace`/`ActiveWorkspace` now carry `logoUrl`; the Settings Workspace section renders the current mark + upload/remove (viewers see the mark read-only). PROVISIONING.md gains the bucket step. No migration. | The **`workspace-logos` public bucket** is an owner-side, one-time Supabase dashboard task (PROVISIONING.md) — like `spec-imports` was; until then upload returns the honest "bucket may not exist yet" and the rest of Settings is unaffected. Brand-profile logos stay a pasted-URL field (their existing pattern); this is the first real Storage *upload* surface after import. Upload happy-path needs the provisioned bucket, so it’s build/typecheck-covered + the Settings baseline E2E (52/52) |
| 2026-06-17 | F6 — field comment threading (M6) | Field comments can now be **replied to**, threaded one level deep. The schema already had `field_comments.parent_comment_id` and `addFieldComment` already accepted `parentCommentId` — this closes the UI gap: `addCommentAction`/`commentSchema` now carry an optional `parentCommentId`, `CommentForm` gains a compact **reply** mode (renders the hidden parent + a one-line composer), and `FieldDetail` threads the flat list — only **root** comments enter the chronological version+comment feed, each rendering its replies (chronological) nested beneath plus an inline reply form. The grouping is a pure generic **`@arther/types`** `groupReplies` (roots vs replies-by-parent, preserves order, surfaces orphaned replies as roots so nothing is dropped) with 4 unit tests (224 total). No migration, no new DB surface (the self-FK insert + RLS were already in place/probed). | Closes the comment-threading slice of F6 polish. Remaining F6 polish — **spec-DB drag-to-reorder** (turning the shared `FieldGrid` server component interactive) and the **searchable reference combobox** — are follow-ups. Threading interactions need a provisioned field, so it stays unit-covered (52/52 E2E unchanged) |
| 2026-06-17 | F6 — searchable reference combobox (M6) | The product “Add component” picker (`AttachComponentForm`) — a plain `<select>` over the whole component library — is now a **searchable combobox**: type-ahead filtering, keyboard, and a11y come free from a native `<datalist>`, with a hidden input carrying the resolved component id (empty until the typed label matches an option exactly, so an incomplete pick fails the existing "Pick a component." validation). New reusable **`Combobox`** client component (specs-local) over a pure **`@arther/types`** `matchOptionValue` (case-insensitive, trimmed label→value) with 4 unit tests (228 total). Lighter and more robust than a hand-rolled dropdown — scales to long reference lists. No migration, no new DB/action surface (reuses `attachComponentAction`). | Closes the searchable-combobox slice of F6 polish. Remaining F6 polish: **spec-DB drag-to-reorder** (needs the shared `FieldGrid` server component to become interactive). The picker needs provisioned library components to exercise, so it stays unit-covered (52/52 E2E unchanged). |
| 2026-06-17 | F6 — spec field reorder (within category) (M6) | Spec fields can now be **reordered** within their category from the grid — the last F6-polish slice. Each field row gains compact **↑ / ↓** controls (the proven embedded-client-form pattern, like `ArchiveToggle`), disabled at the category boundaries. New DB helper `moveSpecFieldOrder` (RLS, editor-write): loads the field’s non-archived same-(owner, category) siblings in `display_order`, moves it one step, and **reindexes the whole group** so the order stays total + gap-free (the grid orders by `category` then `display_order`, so reorder is within-category by construction). New `moveFieldAction` (spec.write-gated) drives it; the array move is a pure, reusable **`@arther/types`** `moveInList` (boundary-safe, non-mutating) with 6 unit tests (234 total). Robust **move-up/down rather than drag** — correct and verifiable in a grouped, interactive table where hand-rolled DnD would be fragile and untestable here. No migration; the `spec_fields` update + RLS surface is already probed. | Closes the F6-polish set (threading + combobox + reorder) and, with it, **the entire buildable-now Phase-1/2 batch**. True drag-and-drop is a follow-up on top of this reorder spine. Remaining work is genuinely gated: **G5.1 presence** (Realtime), **G6.9 variant scaffold** (needs Feature 4), and external provisioning (Trigger.dev G1.2, Resend F4.3, PITR F8.4, the `workspace-logos` bucket). Reorder needs provisioned fields to exercise, so it stays unit-covered (52/52 E2E unchanged). |
| 2026-06-17 | C0.1/C0.2 — document lifecycle state machine + revision creation (Phase 3, M9 kick-off) | First **Phase 3** slice and the next critical-path step (`C0▶C1▶C4▶C5▶C6`): the four-state machine driving `document_revisions.state` (migration 0005 — **no migration**; the column + member-read/editor-write RLS already shipped). **`@arther/types` `document-lifecycle.ts`** (pure, ADR-012): the complete §3.2 transition map (`DOCUMENT_TRANSITIONS` — owner/approver/system actors; `reject` requires a reason), `resolveTransition`/`canTransition`/`transitionActionsFor`, `canManageDocumentLifecycle` (the document owner or a workspace admin — spec §2.4/§4.3), and the C0.4 `submitForReviewSchema` (optional brief + YYYY-MM-DD due date) + `transitionReasonSchema`; 11 unit tests. **`@arther/authz`**: new editor-gated `doc.submit`/`doc.revise` actions (publish reuses `doc.publish`). **`@arther/db` `document-lifecycle.ts`**: `transitionDocumentRevision` — a **guarded conditional UPDATE keyed on the current `from` state** (the G5.1 optimistic-lock pattern, so a concurrent transition can't double-fire; publish stamps `published_at`/`published_by`, submit records the brief/due-date), and `createDocumentRevision` (C0.2) — forks a `draft` working copy from the published revision: clones its blocks (fresh ids) + spec/brief/placeholder references (re-anchored, so staleness/brief tracking carry forward) and moves `current_revision_id`, leaving the published snapshot untouched (spec §2.2/§2.3); `DocumentRevisionRow`/`REVISION_COLUMNS` extended with the lifecycle/submission columns + a `getRevision` read. **App**: a `DocumentLifecycle` client control on the document header (Send for review + brief/due-date, pull back to draft/review, Publish, Create revision — derived from `transitionActionsFor(state,'owner')`), each server action re-authorizing **canDo + document-ownership**; the **Edit** affordance shows only in Draft (a "Locked while in {state}" note otherwise). New `tests/db/document-lifecycle.test.ts` (8 probes — the conditional-transition guard incl. no-double-fire, publish stamping, revision-number uniqueness + pointer move, viewer/stranger can't transition). Gates `pnpm turbo lint typecheck test build` **39/39**, `@arther/types` document-lifecycle 11, drift in sync (no migration). | **Scope held to C0.1 + C0.2 + C0.4.** The approver **approve/reject** (AND-logic, `approval_records`) is **C1** — the map carries those edges but the app wires only owner transitions. **C0.3 server-side edit-lock** (reject block writes when the revision isn't Draft; a structural change to a doc in Review returns it to Draft) is the next slice — today the **Edit** affordance is hidden outside Draft (UI-level) but the editor's write actions don't yet assert revision state. **Publish flips the state + stamps metadata only** — the snapshot resolver + immutable `published_snapshots` write + pre-flight gate are **C4**, wired into this transition there (nothing is portal-served until C6, so a "published" revision has no live snapshot yet). `createDocumentRevision` is a multi-step PostgREST sequence (the createDocument precedent), not atomic — a mid-sequence failure leaves a harmless orphan draft; the atomic RPC is a follow-up. DB probes authored + typecheck-clean, run in **CI's Postgres** (no docker this session). Data-bearing surface → unit/probe-covered (the unprovisioned doc-view E2E baseline is unchanged). **Next: C1 — the approval workflow (AND-logic gate, append-only `approval_records`, reset-on-reject, owner override).** |
| 2026-06-17 | Live progress waffle (tooling) | Added `progress.html` at the repo root — a self-contained, dependency-free dashboard that fetches this plan's §10 session log + §4 roadmap and the four phase task docs **live from GitHub** and renders a waffle chart: one block per task, coloured by phase, with shipped / blocked / not-started inferred from the session-log prose (epic- and milestone-completion aware; milestone→epic mapping read from §4). Includes per-phase + per-epic breakdowns, hover detail, branch selector, and optional auto-refresh. Parser validated against the local docs (193 tasks; P1 52✓/1 gated, P2 49✓/2 gated, P3/P4 not started). | Status is a best-effort read of prose, not a formal status field, so a handful of foundational tasks logged without their ID can under-report; click-to-override (stored per-browser) lets the viewer correct edge cases. A machine-readable per-task status would let the chart drop the heuristic. |
